# RUNDOWN v4 — PLAN: the 9-max seat ladder

Scope agreed 2026-09-07 from README backlog item 16 ("7-max and 9-max seat ladders are deferred to
v4", V3-BRIEF locked 4.8, V3-PLAN §0.2) and this planning session's survey of the tree at `1d988f5`
(`main`, verifier 62/62 green, 679/679 tests, `build.mjs --check` current on both variants, tree
clean, one commit ahead of `origin/main`, nothing pushed). This document is the implementation
hand-off for **one launch**: what v4 contains, why, in what order, what "done" means, and — the
reason the document exists — how a single `Workflow` run carries the whole thing from precheck to
commit while the launching session's context stays flat (§7).

It follows the same rules as [METHODOLOGY.md](METHODOLOGY.md) and [V3-PLAN.md](V3-PLAN.md): the
Monte Carlo layer is objective, the scoring layer is opinion, every new constant gets named,
anchored, shipped in `constants`, rendered by the Method view and bounded by a gate. Where this
document and METHODOLOGY disagree, **METHODOLOGY is right**.

**Annotation convention.** Executors annotate this file in place as stages land, exactly as
V2-PLAN and V3-PLAN were annotated: `> **Measured (stage N).**` blocks under the prediction they
confirm or falsify, resolutions appended under §10, reversals recorded rather than edited away.
The plan text above the annotations is kept as written.

**Organizing principle, carried over: the gate catalog is designed before the feature.** A v4
mechanism without an invariant asserting its claim is not done. §5 is the plan's core; §2 is the
code that makes §5 true; §7 is the machine that runs it.

---

## 0. Scope

### 0.1 In

One feature: **a table-size axis, `env.seats ∈ {6, 9}`, default 6**, with the nine-seat ladder
`UTG · UTG+1 · UTG+2 · LJ · HJ · CO · BTN · SB · BB` live on every surface the six-seat ladder is
live on today — the position rail, all four nodes, the VPIP slider, depth / rake / straddle, the
villain profile, the skill dial, the EV mode, the sub-cell top-N, hand search, Simulate, the
Method view — in **both** variants. Plus, as plan-mandated infrastructure:

- The **ladder generalisation** of `scripts/lib/policy.mjs` (§2.1–2.2): one canonical nine-seat
  array, every seat-keyed table replaced by a structural function of ladder index, the two named
  seat-literal offenders retired. This is the load-bearing refactor and it lands **before**
  fan-out, byte-identical at `seats = 6`.
- The **ring artifact** `data/ring.json` (§2.4): the opponent-count columns `N = 8, 9` for every
  cell, generated by its own script with its own seeds, `--check`-gated byte-identical like the
  checkdown matrix and the equilibrium, and consumed **only** at `seats = 9`.
- The **fourth tier fixture** `data/tiers-9max.fixture.txt` (§2.5) via a new `freeze-tiers.mjs`
  kind — created, never `--force`d.
- The gates of §5 (I48–I52, D12, D13), the METHODOLOGY / README repairs of §6, and the
  single-launch orchestration script `.claude/workflows/v4.js` (§7).

### 0.2 Out — decided, not deferred by accident

- **7-max and 8-max.** The axis is the set `{6, 9}`, not an integer. Every additional table size
  is a fixture ceremony, a skill-exception re-measurement and a UI surface of its own; nothing
  here builds a ladder generator that pretends otherwise. The code keys off `seats` so that a
  later `{6, 7, 9}` is a data change, but no 7-seat list, fixture or control ships.
- **Any multiway baseline.** The HU-only equilibrium (P3) and its "baseline is HU" reasons stand;
  the 12 new (position, node) pairs — `UTG|rfi`, `UTG|3bet`, `UTG1|{rfi,limps,raise,3bet}`,
  `UTG2|{rfi,limps,raise,3bet}` and, because the "first seat of the ladder" exclusion moves
  forward, `LJ|limps` and `LJ|raise` (§2.6) — join the 21 rows already uncovered in vs-GTO mode,
  for **33 of 36** at nine seats (I36(d)'s denominator is all pos × node, not the legal subset:
  3 of 24 covered at six seats, 3 of 36 at nine). The `SIXMAX`
  deferral record in `scripts/lib/cfr.mjs:901` is **renamed** to a table-size-neutral name and its
  prose corrected (§6); its reasoning (I35(d), no k-way sampler) is untouched and applies to nine
  seats exactly as it does to six.
- **Any regeneration of the existing measurement layer.** `cells[*].eq[0..6]` and
  `cells[*].vDelta[*][0..6]` stay byte-identical; the ring artifact is additive (§2.4). D8, I24,
  I25 untouched; `orderHash` unchanged.
- **Any change to the three frozen baselines.** I22, I32 and the v3-default fixture must be green
  at the end of the run **with no `--force` anywhere in the run's history** — this is the v4
  identity constraint (§0.4), and it is what makes "9-max" a feature rather than a re-freeze.
- Mobile / narrow-viewport layout; 5-card PLO; the rest of the v3.1 backlog (README items 1–15,
  17), which this plan neither schedules nor touches except where §6 says so.
- Hand-editing `index.html` / `index-full.html` — generated; edit `src/shell.html` and rebuild.

### 0.3 Non-negotiable carry-overs

- **GREEN means all three**: `node scripts/verify.mjs` exit 0 with every gate pass; `node --test
  test/*.test.mjs` all pass; `node scripts/build.mjs --check` current on both variants. Plus, at
  the close: `node smoke.mjs` 2/2 with the morph rows unchanged against their 8/16/4 ms budgets,
  `node browsers.mjs` 2/2, and both `--check` generators (`generate-checkdown-matrix.mjs`,
  `generate-equilibrium.mjs`) byte-identical, joined by the new `generate-ring.mjs --check`.
- Constants need anchors (V3-PLAN §6). Unanchorable → gated + flagged + badged `estimate`, or not
  shipped. The red team (§7.4) attacks every new number.
- Gates are written to FAIL; tolerances are never widened to pass. Byte ceilings tighten-never-
  widen except as a **paid raise** under D6's from-above clause (§2.7).
- `scripts/freeze-tiers.mjs` is the sole fixture writer; `--force` is not authorised anywhere in
  this plan.
- Commit only at the boundary, only when green, never push. Browser testing headless with
  throwaway profiles only.
- No new runtime or dev dependency (README item 15).

### 0.4 The v4 identity constraint — the seat axis is inert at `seats = 6`

V3-PLAN §0.4 admits a new mechanism in one of three ways: (a) a new axis inert at legacy
settings, (b) a new artifact, (c) a deliberate re-freeze. v4 uses (a) for the axis and (b) for
the ring artifact, and **forbids (c)**. Concretely:

- At `seats = 6`, every painted tier, every score, every disabled reason, every Method-view line
  and every byte of `data/model.json` outside the new `constants.ladder` block is identical to
  `1d988f5` — the ring is a **separate artifact**, `data/ring.json`, not a `model.json` sub-block
  (§2.4). Proven three ways: the three legacy fixtures green with an empty `--check` diff; gate
  I48's tripwire (zero `data/ring.json` on a copy → the seats-6 surface does not move); and D11's
  dual determinism holding across the rebuild.
- The 6-max ladder is **not renamed**: `UTG · HJ · CO · BTN · SB · BB` remain the keys of every
  legacy fixture row, URL state and gate id. The nine-seat ladder gives the same structural seat
  the name `LJ` (§2.1), and gate I50 is where that equivalence becomes a claim that can fail.

> **Measured (stage S6, the fix round) — the first bullet's claim, stated as a residual, and it
> needed two repairs before it was true.** Diffed field by field against `1d988f5`, `data/model.json`
> now differs in exactly **ten paths**: `meta.hash`; the seven §5.2 verdict stamps
> `gates.{I48, I49, I50, I51, I52, D12, D13}`; `constants.limitations` (§6's limitation 20, appended
> as item `[2]`); and `constants.ladder` itself — the block the bullet excepts. `cells`, `rows`,
> `order`, `baselineTiers`, `calibration`, `bands` and `benchmarks` are **byte-identical whole
> blocks**, so every `eq` and `vDelta` is untouched — 123 of `cells`' 145 entries carry those two
> keys, and all 145 entries compare equal as one block. The residual is the v3 P-stage
> precedent exactly: a provenance hash plus verify's own stamps of the deliverables this plan asked
> for.
>
> **The two repairs, because until S6 the claim was FALSE in two places and the verification round
> caught it.** (i) `envKey` appended `|${e.seats}` unconditionally, `evDefaultKey` embeds `envKey`,
> and `constants.evCut.derivedAt.state` **ships** that string — so a `|6` reached `data/model.json`
> at legacy settings. Repaired by serialising the seat segment only when it is not 6: the axis stays
> **in** the key (removing it would hand a table size back the other size's memoised answer, the trap
> `envKey`'s own docstring is about), the legacy string is byte-identical to v3's, and nine stays
> distinct. Inertness at six is not absence from the key. (ii) `constants.straddle.seatDerivedFrom`
> was a v4 provenance field sited **outside** `constants.ladder`; it moved to
> `constants.ladder.anchorSharedWith: 'straddle.seat'` (668 B block, cap 1K), taking I51(a)'s
> by-value assertion with it — both clauses kept, plus a violator test the string clause never had.
> Bytes crossed *out* of `metaCore` *into* a block `metaCore` subtracts, so no ceiling moved.
>
> **And the run's last model-touching act is the ring re-stamp.** `ring.meta.model.hash` pins
> `data/model.json`'s whole-file hash, so every stamp above invalidated `generate-ring.mjs --check` —
> the standing cost §2.4's finding and METHODOLOGY §3.6 record, which S5's §5 finding 1 had already
> called and prescribed the remedy for. `data/ring.json` was regenerated **after** the last stamp,
> against a sha256 fixed point proved by three consecutive verifies, in **1,156.9 s against R2's
> 1,280 s** (faster than S3's 1,213 s, so the margin widened), and **not one measured column moved**:
> all 123 `cells` byte-identical, `orderHash`, `generatorHash` and `kernelHash` unchanged, and only
> `meta.model.hash`, `meta.contentHash` and `meta.wallSec` different — +2 B on disk. Verify was then
> re-run and the fixed point re-read unchanged, so the gap did not re-open. Readings in
> `docs/spikes/V4-S6-verification.md` §6.

---

## 1. What the survey found — the facts this plan stands on

The conclusion first: **the expensive layer is position-free, so 9-max is a policy / UI / fixture
change, not a measurement.** `data/model.json` (141,374 B) is keyed by hand class × suitedness —
29 rows × 5 columns = 145 cells — and each cell carries `eq[N]` for `N = 1..7` opponents and
`vDelta[v][N]` over the 5-point villain lattice × 7. There is no position axis in the data at all.
Position enters client-side through a handful of small tables:

| What | Where | Note |
|---|---|---|
| Canonical list `POSITIONS` | `scripts/lib/policy.mjs:12` | imported by 15 files (a 17-file `grep -rl` over `scripts/ src/ test/` minus the definition itself and `src/shell.html`'s hand-typed duplicate) — the good pattern |
| Hand-typed duplicate `var POSITIONS` + `NNB` / `NBL` / `legalPos` / `posDisabledReason` / `seatsBefore` | `src/shell.html:2182–2199` | the pre-injection fallback path; a second thing to update |
| Seats-still-to-act tables `N_NB` / `N_BL` | `policy.mjs:477–478` | duplicated in the shell at :2183–2184 |
| Nesting chain `NEST_CHAIN` (per node) | `policy.mjs:480` | re-typed as literals in `scripts/gates/couplings.mjs:634`, `gates/env.mjs:195`, `gates/policy-sweep.mjs:37`, `scripts/lib/equilibrium.mjs:710`, `src/shell.html:3202` |
| Full six-seat literal in `domainLabelFor` | `scripts/gates/baseline.mjs:247, 353` | |
| Per-seat opinion constants `baseR`, `baseRaise` | `policy.mjs:38`, `:384` | six-entry objects keyed by name — the only per-seat **numbers** |
| The two seat-literal offenders V3-PLAN §0.2 names | `policy.mjs:1406` (`BTN\|SB\|BB` limp-width branch), `:1914` (`heroIP = CO\|BTN`) | must become structural predicates |
| Opponent-count ceiling `NMAX = 7` | `scripts/lib/mc.mjs:37` (`NMAX_V1 = 5` at :40); `PLO_MC.NMAX` in `scripts/lib/sim-kernel.js:28`; `meta.nMax`; **the page's own pair `src/shell.html:3156` `var N_EFF_MAX = 7`** — policy's clamp literal, deliberately distinct from `:1181` `var NMAX = meta.nMax` (the array bound) and consumed at `:3175`, `:3545`, `:3557`, `:5541`, `:8620`, `:8956` | the numeric constants tied to table size — **four** sites, not one |
| The clamp `Math.min(7, …)` + `extrapolated: raw > 7.0001` | `policy.mjs:806–807` | at 6-max, raw `N_eff` maxes at 8.23 and clamps in 1.19 % of settings (V3-BRIEF :211–213, the census over 3,960 UI-reachable settings; `> 5` in 14.5 %, `> 6` in 5.2 % — the locked-decisions table two hundred lines earlier is not the census) |
| Measured skill-dial exception lists keyed `'pos\|node'` | `scripts/lib/skill.mjs:111, 142` | **measured, not derivable** — the 12 new pairs (`UTG\|rfi`, `UTG\|3bet`, `UTG1`×4, `UTG2`×4, `LJ\|limps`, `LJ\|raise` — §0.2) need the same measurement |
| The straddle's "one seat tighter" | `policy.mjs:367–372` | `seat = 0.77 = 1/√(1.250·1.350)`, derived from `baseRaise`'s UTG→HJ and HJ→CO steps — the model's only anchored seat step |

What does **not** depend on table size: the straddle transform (multiplicative, generalises once
the ladder has nine entries); rake and pot arithmetic in `derived()` (blinds posted are always
two); the checkdown matrix and the HU equilibrium; the calibration verdict; the Simulate engine's
seat loop (it is opponent-count-based, `PLO_MC.NMAX`, not seat-name-based).

What is tight, on the page: `appCore` 359.3 / 360 KB (682 B headroom), `blocks.skill` **3.4 / 4 KB
(~560 B)** — D6 reads `skill 3.4K/4K≤4K` and `variant.mjs`'s `budgetSource` records **3,532 B**
measured at P4 — and `modelCode` 52.1 / 54 KB; all three are `scripts/lib/variant.mjs:128–129`,
mirrored for full at `:195–196` (`:136` is a `budgetSource` prose line D6 cites by phrase, not a
budget). On the shared data artifact: `model.json`'s `core` 116.0 / 120 KB and `total`
138.1 / 145 KB, which live in `scripts/gates/data.mjs:584–592` (`BUD` / `CORE_BUDGET`), **not** in
`variant.mjs` (METHODOLOGY §9.11). And on the full variant, `total` 653.7 / 660 KB — 6,478 B
(§2.7). §2.7 says where every new byte goes so that none of these is raised by accident.

Generator cost: the pipeline runs in 188 s against the hard budget METHODOLOGY :1954–1958 states
as **"6 minutes"** (README :370 says only "a couple of minutes" and carries no figure). **Both**
`NMAX`-scaling stages matter — S2 (12 s) and S2L (101 s) — because every trial in both kernels
draws `NEED = 5 + NMAX * 4` cards (`scripts/lib/mc.mjs:106`); `scripts/generate-data.mjs:219`'s
`draws = jobs × TRIALS.latt × NMAX` is the lattice half of it. The ring artifact is a **separate
script** precisely so that this budget is not touched (§2.4).

---

## 2. The design, decided

### 2.1 The ladder — one array, two views, no new literals

`policy.mjs` gets one canonical constant and one derivation:

```
LADDER9   = ['UTG','UTG1','UTG2','LJ','HJ','CO','BTN','SB','BB']     // keys; display 'UTG+1', 'UTG+2'
seatsFor(6) = ['UTG','HJ','CO','BTN','SB','BB']                        // the legacy list, verbatim
seatsFor(9) = LADDER9
```

`POSITIONS` stays exported and **equal to `seatsFor(6)`** so the 15 importers and every fixture
row are untouched. The structural fact the two views share: a seat is defined by **how many
non-blind seats and how many blind seats act behind it**, and 6-max `UTG` (3 behind, 2 blinds) is
structurally 9-max `LJ`. The name `LJ` is chosen (over `MP`) because it makes that identity
legible on the rail: the six-seat table is the nine-seat table with its first three seats empty.

Rule (V3-PLAN §0.2, now enforced by gate I51(c)): **no new seat-name literal anywhere** — not in
policy, gates, tests, the shell, or docs prose that the gates read. Every consumer takes the list
from `seatsFor(seats)` or a structural function of it. The five re-typed `NEST_CHAIN` literals
and `baseline.mjs`'s two six-seat arrays are replaced by imports in the same stage.

### 2.2 Structural functions replace the name-keyed tables

| Today | v4 | Identity check at `seats = 6` |
|---|---|---|
| `N_NB[pos]`, `N_BL[pos]` | `behindNonBlind(pos, seats)` — the **existing** helper at `policy.mjs:764`, extended with `seats` rather than duplicated under a transposed name — and `blindBehind(pos, seats)`, both from ladder index | returns the :477–478 table exactly; `behindNonBlind`'s one caller, the `raise` branch of `nEff` (`:800`), is unchanged in meaning |
| `NEST_CHAIN[node]` | `nestChain(node, seats)`: rfi = all non-blind seats in ladder order; limps / raise = the same minus the first seat; 3bet = `[]` | returns :480 exactly |
| `positionDisabled(pos, node)` | `positionDisabled(pos, node, seats)`: BB closes rfi; the **first seat of the ladder** has nobody in front, so `limps` and `raise` are structurally unavailable there | the same 3 exclusions → 21 legal pairs at 6, **predicted 33 at 9** |
| `policy.mjs:1406` `BTN\|SB\|BB` (inside `width3For`) | `behindNonBlind(pos, seats) === 0` — "nobody non-blind left to act": BTN, SB, BB at 6 **and at 9**. One disjunct, not two: `blindBehind < 2` is redundant (`N_NB[SB]` and `N_NB[BB]` are 0 at every table size, so it never fires independently) and stating it would make the predicate say something other than its gloss | same three seats at 6 |
| `policy.mjs:1914` `heroIP = CO\|BTN` | `behindNonBlind(pos, seats) <= 1 && !isBlind(pos)` — "at most one non-blind seat behind": CO, BTN at 6 and at 9 | same two seats at 6 |
| `Math.min(7, raw)`, `raw > 7.0001` | `Math.min(nMax(seats), raw)`, with `nMax(6) = 7` **frozen** and `nMax(9) = 9`; the `N = 8, 9` columns are reached through the new accessor `eqAtSeats(cell, N, seats)`, never by splicing `cells` (§2.4) | the clamp at 6 does not move — the 1.19 % of clamped legacy settings stay clamped at 7, and `eqAtSeats` is never called with `N > 7` at `seats = 6` |
| `src/shell.html:2182–2199` fallback copies | the shell reads `window.POLICY.seatsFor` and the structural functions; the fallback is reduced to the six-seat list only, marked legacy | shell output at 6 byte-identical (D11) |

> **Measured (stage S5, from S1 and lane K).** **CONFIRMED, and the count is exact.**
> `positionDisabled(…, 9)` disables three pairs of the thirty-six and they are the *same three
> structural facts* as at six — `BB|rfi`, `UTG|limps`, `UTG|raise` — so **33 legal pairs at nine
> seats against 21 at six**, `{rfi 8, limps 8, raise 8, 3bet 9}`, and **no node carries a second
> structural exclusion** the six-seat table never exposed (§10 question 1, answered). Both offender
> rewrites reproduced their literal sets at six and named `{BTN,SB,BB}` / `{CO,BTN}` at nine, so
> neither literal had to be kept: `offendersRewritten = 2`, asserted by I51(b). One refinement the
> plan did not predict, recorded because a name-keyed procedure would trip on it: "the twelve new
> pairs" has **two** derivations that are not the same twelve — by KEY (`legalPairs(9)` minus
> `legalPairs(6)`: `UTG1`/`UTG2`/`LJ` × four nodes) and by LADDER POSITION (§3 R5's enumeration,
> which carries `UTG|rfi` and `UTG|3bet` instead of `LJ|rfi` and `LJ|3bet`). They differ by two
> pairs each way, both contain `LJ|limps` and `LJ|raise`, and the shipped skill records cover **all
> 33** rather than either twelve — the only self-consistent answer. Both are pinned in
> `test/ladder.test.mjs` so the disagreement cannot be quietly resolved.

The two offender rewrites are **predictions**: that the structural predicate reproduces the
literal's seat set at 6 *and* that it names the right seats at 9. Both halves are asserted by
I51(b). If the executor finds that a predicate cannot reproduce the literal at 6, the literal
stays, is recorded as an offender in METHODOLOGY, and I51(b) records the exception — it is not
laundered into a predicate that happens to match.

### 2.3 The early-seat constants — derived by rule, not typed

`baseRaise` and `baseR` need values for `UTG`, `UTG1`, `UTG2` at nine seats. **Nobody types a
number.** Each is derived from a named rule whose anchor already exists in the model, the rule is
itself a constant in `constants.ladder`, and the derived values are printed in the Method view
beside the rule that produced them.

`baseRaise` — the opening ladder. The model already owns one seat step: `straddle.seat = 0.77 =
1/√(1.250 · 1.350)` (`policy.mjs:367–372`), the geometric mean of the two early steps of the
ladder, explicitly excluding the CO→BTN step. v4 names that number `ladder.earlyStep`. It stays
**two constants with equal values, one anchor** — `straddle.seat` keeps its own value and gains
`derivedFrom: 'ladder.earlyStep'`; it does **not** become a live reference. A live reference would
couple I26, whose whole job is to perturb `straddle.seat` (that is how the "BTN keeps its 0.45"
claim was falsified — the `seatPinned` comment at `policy.mjs:373–378`), to the opening ladder:
a perturbation above 1 would invert the nine-seat `baseRaise` ladder that I51(a) requires to be
strictly increasing, and the two gates would fight. Instead I51(a) asserts the equality
`straddle.seat === ladder.earlyStep` on the **shipped** constants, so they cannot drift, and I26's
perturbation is exempt from it by name and does not flow into the ladder. Note also that
`straddle.seat` is a **width** factor, not a realization factor: `seatWidthFactor` (`:749–753`)
returns it for every seat and multiplies `baseRaise[pos]` in `widthFor` (`:1394`). The rule:
`baseRaise(UTG2) = baseRaise(LJ) · earlyStep`, `UTG1 = LJ · earlyStep²`, `UTG = LJ · earlyStep³`.
What the rule yields — **recorded here as the rule's output, not as chosen numbers**: with
`baseRaise(LJ) = 0.16` (the legacy UTG value, unchanged), `0.1232 · 0.0949 · 0.0731`. Whether a
7.3 % nine-handed UTG open is defensible is exactly the question the red team is asked (§7.4);
the plan's position is that a geometric ladder is the only anchor the model has, and an
unanchored "looks right" override is what §0.3 forbids.

`baseR` — the per-seat realization factor (`0.97 · 0.99 · 1.02 · 1.06 · 0.90 · 0.93`,
`baseRealization(pos, d) = baseR[pos]^(1 + beta·u(d))` at `policy.mjs:873`, consumed by
`realization` at `:879`). Its seat-dependence is small and non-monotone, and the model records no
measurement that distinguishes one out-of-position early seat from another. It is **not**
scoring-only: `baseR` also moves painted *width* at non-reference depth through
`depthWidthFactor`, which is the ratio of two `baseRealization` calls (`policy.mjs:634`) applied
as `g` in `widthFor` (`:1389`, `:1398`). The v3-default fixture's 12 lanes include d = 40 and
d = 250, so R1's flat-vs-step choice moves painted width in **8 of the 12 lanes** — for the new
seats only — and is inert only at d = 100, where `g === 1`. I51 must read both surfaces.

> **Measured (stage S4).** **FALSIFIED, and recorded rather than patched.** R1 moves painted width
> in **0 of the 12 lanes**, not 8: `TF3.DEFAULT_LANE` (`scripts/lib/tier-fixture-v3.mjs:59`) sets
> `depthWidth: false` on *every* one of the twelve, so `depthWidthFactor` returns exactly 1 in all
> of them and never reads `baseR` at all. Three refuters of three shipped `baseRRule: 'step'` and
> got a completely green tree — verify 69/69, the whole test suite, `build --check` current, and
> I49's **3,214,728** nine-seat tiers **byte-identical** — because `baseR` enters scoring as one
> per-seat common factor over a whole (pos, node, v) table, a common factor cannot re-rank, and
> tiers are rank-based. The choice is live only with `depthWidth` **ON** (UTG at d = 40: 0.075120
> flat against 0.076818 step, +2.3 % of painted width; 40 of 84 width differences and 12 of 84
> painted-set differences over a sampled sweep), a UI surface no fixture freezes. So "decided by
> measurement" is false as written: no shipped surface can distinguish the two rules, the
> tie-break to flat is a **preference** — correctly labelled as one — and R1's constant ships
> gated (I51(a) admits `'flat'` or `'step'` and nothing else), flagged (`constants.ladder.flag`
> names `baseRRule`) and **badged** (`UNANCHORED['ladder.baseRRule']`, gated by I51(a)'s badge-map
> clause). docs/refutations/V4.md.
**Two candidate rules, decided by
measurement, not by preference** (§3 rule R1): (flat) the three new seats inherit `baseR(LJ)`;
(step) they continue the UTG→HJ step downward (`0.95 · 0.93 · 0.91`). Decision rule: run I51's
nesting and monotonicity checks under both; if only one passes, it ships; if both pass, **flat**
ships (fewer new numbers, and no claim the model cannot back), and the step rule is recorded as
the rejected alternative with its readings. Either way `constants.ladder.baseRRule` names which.

Both rules are **opinion**. They ship `kind: 'estimate'`, badged in the Method view, bounded by
I51, and the red team's memo on each is committed to `docs/refutations/V4.md`.

> **Measured (stage S4).** The three legs were *present* and two of them were **unasserted**, which
> is the P1/P3 defect repeating one artifact later. (i) `constants.ladder.derived` is copied out of
> `LADDER_C9` at module init and was then compared with nothing: a refuter typed a table over it
> after the computation and shipped every ring gate, all 12 ladder tests and a full verify green,
> with the Method view printing numbers the live ladder does not run. (ii) `baseRaiseRule` is a
> LABEL `ladderConstants` never reads — the geometric step is written into the loop — so an
> arithmetic or linear ladder under the name `'geometric'` passed I51(a) **in full**, monotonicity
> clause included, and only the frozen fixture objected. (iii) The badge itself was carried by a
> regex in `test/ui-seats.test.mjs`, weaker than both precedents §6 cites. I51(a) now composes the
> ladder from the **named** rule character-for-character as `LADDER_C9` spells it and
> `Object.is`-compares it against what shipped (the I41(f) idiom), compares the published block
> against `ladderConstants(9)` seat by seat, re-derives `earlyStep` from the live `baseRaise` table
> at the published rounding, asserts `anchor`'s own arithmetic and `flag`'s named constants by
> content, and asserts both badge-map entries and the reader that consumes them.

### 2.4 N_eff above seven — the ring artifact

At nine seats `N_eff` exceeds 7 routinely: rfi from UTG at `v = 0.90` gives `raw ≈ 5.54`, and the
limps node reaches `raw ≈ 8.24` at **UTG+1** over three limpers — UTG itself is structurally
disabled at that node (§2.2), so the 9-max UTG limps reading is not a legal setting. Over **four**
limpers, which is UI-reachable and is where 6-max already peaks (V3-BRIEF :211: max raw 8.23 at
limps/HJ, VPIP 90, 4 limpers, straddle), UTG+1 reads **9.14**, and **9.96** straddled — so
`nMax(9) = 9` does not remove the clamp, it moves it, and R3's census measures the residue.
Clamping at 7 would badge a large share of the new surface `extrapolated`; regenerating
`model.json` at `NMAX = 9` would add **~7.0 KB** — measured, not estimated: `eq[]` is 4,242 B over
861 numbers and `vDelta[]` 20,833 B over 4,305 numbers, i.e. 4.9 B per number over the **123
non-empty cells** (22 of the 145 keys are structurally empty, METHODOLOGY §2.3), so two more N
columns is `123 × 12 × 4.9 ≈ 7,164 B` — taking `core` from 116.0 K to ~123 K past its 120 KB
ceiling and re-measuring the frozen layer. Neither is acceptable, so:

**`data/ring.json`** — a new artifact in the P3 idiom (`checkdown-matrix.json`, `equilibrium.json`):

- Generated by **`scripts/generate-ring.mjs`** (new; library in `scripts/lib/ring.mjs`), which
  deals nine villains per trial with the same `TRIALS` and `se` regime as `generate-data.mjs`
  (`meta.trials.cell = 100000`, `se.cell = 0.16`; lattice likewise) under **two named seeds**
  `rundown-v4/ring-A` / `-B`, and writes per cell `eq[7..8]` and `vDelta[v][7..8]` (0-based:
  the `N = 8, 9` columns) plus `meta { generatorHash, contentHash, seeds, trials, se }`.
- The prefix technique yields `N = 1..7` from the same deals for free. Those columns are **not
  shipped**; they are the artifact's falsifier: D12(c) asserts they agree with the shipped
  `cells[*].eq[0..6]` within `2 · se.cell` on every cell — an independent, differently-seeded
  reproduction of the v1 measurement layer, obtained at no extra cost.
- `--check` re-runs the generator and refuses to write on any byte difference (D12(a)), exactly
  as the matrix and equilibrium generators do.
- Wall time is **its own budget**, pre-registered at **300 s** — derived, not chosen: the shipped
  pipeline's two `NMAX`-scaling stages are S2 (12 s) + S2L (101 s) = 113 s at seven villains
  (METHODOLOGY :1955), cost scales ≈ linearly in villains because the per-trial draw is
  `NEED = 5 + NMAX * 4` (`scripts/lib/mc.mjs:106`), and the ring runs **two** seeds:
  `2 × 113 × 9/7 ≈ 291 s`, rounded up to 300. It is measured at stage S2 and written into
  METHODOLOGY. It is **not** added to `generate-data.mjs`'s 188 s.

> **Amended by the owner (2026-09-08, after run 1 — stage S2, lane R).** The derivation above is
> **falsified by measurement** and is kept as written. Linear-in-villains holds for `runMulti`
> (per-cell 19 → 22 ms, **1.16×**) and fails for `runMultiFiltered` (per-cell lattice
> Σ 189 → 1,170 ms, **6.2×**): by the ninth villain 40 of 52 cards are dead, the VPIP-filtered
> pools exhaust rejection sampling, and each exhaustion burns the full `RANGE_TRIES = 4,000`
> before falling back to a random hand (v = 25 fallback rate 0.029 % → 5.744 %). End to end the
> ring's two lattice stages ran 584.1 s and 599.1 s against the shipped S2L's 101 s (5.9×,
> reproducing the per-cell ratio within 5 %); the two-seed wall was **1,213.6 s** at four workers.
> R2's halving would give ≈ 622 s and eight workers gave 660.8 s, so neither meets 300 s and R2's
> blocker clause fired as written. The budget is therefore **re-derived from the measured cost
> model, stage by stage, in the same shape as the derivation it replaces**:
> `2 × (12 × 1.16 + 101 × 6.2) ≈ 1,280 s`, pre-registered at **1,280 s**, measured at four
> workers — the derivation's own regime; METHODOLOGY records that worker count cannot move a
> number, so a larger count is a wall-clock trade, never a way to meet the budget. The lattice
> ships at the full `generate-data` regime: no halving, no seed dropped. It remains a one-off cost
> per change to the construction, never per model run or per verify, exactly as the checkdown
> matrix's 21 s is. Run 1's evidence: `docs/spikes/V4-ring.md` §3.2 and §7 (lane R, branch
> `worktree-wf_3c958a35-f26-4`). The finding beneath the number — 5.7 % of the ninth villain's
> v = 25 draws are not from the range at all — is a measurement-layer limitation METHODOLOGY
> carries beside limitation 20, not something the budget launders.

> **Measured (stage S5, from lane R run 2 and the S3 regeneration).** The amended budget holds and
> the original does not. **Two-seed wall 1,255.4 s at four workers** — 0.98× of the amended
> **1,280 s**, and **4.2×** the pre-registered **300 s**, which stays falsified as written. The
> deterministic regeneration on the merged tree read **1,213.0 s** and is what `meta.wallSec`
> records; `--check` rebuilt the artifact byte-identically at a *different* worker count (8, in
> 630.3 s), and the blocked-pool fallback counts reproduced **to the unit** — 5,195,877 and
> 5,210,970 — across four independent runs, which is the determinism claim making itself rather
> than being asserted. D12(e) judges the measured wall against `ring.meta.wallBudget` and is green.
> The rejection-exhaustion finding the amendment names is carried in METHODOLOGY §3.6 beside
> limitation 20, as the amendment required: at `v = 25` the ninth villain's fallback rate is
> **5.744 %** against 0.029 % at seven villains.

- The build embeds it **as its own injected payload**, exactly on the `data/equilibrium.json`
  precedent (the full variant's `eq 73K` region, printed by the build census as its own region):
  a top-level `ring: N * 1024` budget row in **both** `VARIANTS.lite.budgets` and
  `VARIANTS.full.budgets` (`scripts/lib/variant.mjs:128–129`, `:195–196`), pinned from above by
  **D12(d)** (§2.7). It is **not** a `model.json` sub-budget: `model.json` grows only by
  `constants.ladder` (a few hundred bytes, absorbed by `core`'s existing headroom), so D6's `BUD`,
  `CORE_BUDGET` and `core` at `scripts/gates/data.mjs:584–592` need **no edit** and `core`
  116.0 / 120 KB is genuinely not raised. The page-side markup is the separate `@block:ring`
  (§2.7). It is read only by `nMax(9)`'s consumers; at `seats = 6` the accessor is never called
  (I48's tripwire). It ships in **both** variants — lite keeps Simulate (V3-PLAN §5.2), and
  Simulate at nine seats needs `PLO_MC.NMAX = 9`.

**How the extra columns are read.** The ring is *not* spliced into `cells[*].eq`; `eq[]`,
`vDelta[]` and `orderHash` stay byte-identical (§0.2). `policy.mjs` gains one accessor,
`eqAtSeats(cell, N, seats)`, which delegates to today's `eqAt` for `N ≤ 7` and reads the ring
payload's `N = 8, 9` columns above it; the same for `vDelta`. It is never called with `N > 7` at
`seats = 6`. `meta.nMax` stays 7 in `model.json` (it describes `cells`) and the page's `NMAX`
(`src/shell.html:1181`) stays 7 with it, because that name is the **equity-array shape
invariant**, enforced at `:1318` (`validEqArray`, `e.length !== NMAX`) and `:1407`
(`p.nMax !== NMAX`, sim-payload compat) — a 9-length `eq` would fail it. A separate name,
`SIM_NMAX = nMax(seats)`, is what `PLO_MC.NMAX` is set from; `validEqArray` gains a second arity
so the sim path validates against `SIM_NMAX` and the model path against `NMAX`.
`ring.meta.nMax = 9`. Lane R owns both names, and I52's tripwire is written against the accessor,
not against `cells`.

`mc.mjs`'s `NMAX` becomes the **default of a per-call `nMax` option** threaded through **both**
kernels (`:82–144` cell, `:168–229` lattice) — the buffers (`Float64Array(NMAX)`,
`Int32Array(NMAX*4)`) and the draw width (`:106` `NEED = 5 + NMAX * 4`) all size from it — and
`sim-kernel.js:28`'s IIFE-time `var NMAX = PLO_MC.NMAX` becomes a per-job field so the page passes
`nMax` with the job. The `@worker-slice-start … @worker-slice-end` region (`mc.mjs:30–34`) stays
byte-sliceable and `test/sim-bundle.test.mjs` stays green; that is lane R's acceptance test.
Because `NEED` changes with `nMax`, dealing nine villains changes the RNG stream, so the ring's
`N = 1..7` prefix is a differently-seeded reproduction of the v1 layer rather than a byte-identical
one — which is exactly what makes D12(c) a falsifier.

### 2.5 The fourth tier fixture

`data/tiers-9max.fixture.txt`: the v3-default environment surface (12 lanes of depth {40, 100,
250} × rake {0, preset} × straddle {off, on}, villain profile ON, every other axis at its
default) at `seats = 9` — predicted **33 legal pairs × 66 VPIP × 12 lanes = 26,136 settings**,
roughly 0.8 MB. Written by a fourth `KINDS` entry in `scripts/freeze-tiers.mjs` (`--seats9`),

> **Measured (stage S3).** **Confirmed exactly.** The ceremony ran once, with no `--force` anywhere
> in the run's history, and printed **26,136 settings x 123 cells = 3,214,728 tiers** — 33 legal
> pairs x 66 VPIP x 12 lanes, the prediction to the setting. The file is **833,021 B = 813.5 KB**,
> inside lane F's measured bracket (809.2–844.6 KB) and inside "roughly 0.8 MB".
> `--seats9 --check` reproduces with an empty diff and I49 is green. **Zero settings refused** —
> lane F measured 720 refusals (2.755 %) at S2 and named the two causes; S3's policy deltas F1 and
> F3 closed both, so the domain froze whole rather than with a hole in it.


via `scripts/lib/tier-fixture-9max.mjs`, **created** at stage S3 (the file must not exist before
the run; the writer refuses to overwrite without `--force`, and `--force` is not authorised).
Gate **I49** compares every run against it. The freeze prints, and stage S3 commits into
METHODOLOGY, the **sub-ladder diff**: for every shared seat where the pair is legal at both sizes
(§5.2 I50), which (node, VPIP, cell) readings differ between the 9-max row and its 6-max
counterpart, plus the count of cells the nesting post-pass unioned into that seat — the evidence
I50 is scored on.

The kind selector at `scripts/freeze-tiers.mjs:150`
(`const kind = KINDS[flag('v3') ? 'v3' : flag('v2') ? 'v2' : 'v1']`) gains a `--seats9` branch;
the three existing `KINDS` entries are untouched, and `--check` is already per-kind
(`:161–171` uses `kind.load` / `kind.compare`). **I49 is a gate, unlike the v3-default fixture,
which deliberately took none** (`scripts/lib/tier-fixture-v3.mjs`'s header: "NO NEW GATE ID …
Inventing an id here would defeat the point of reserving them", and `KINDS.v3.gate` is the literal
string `'(no gate — pinned by test/tier-fixture-v3.test.mjs)'`). The divergence is deliberate and
turns on the condition that header names: §5 reserves I49 *before* the fixture exists, which is
what V3-PLAN §7.2 asked for and what the v3 fixture — whose plan reserved no id for it — could not
claim.

### 2.6 The UI

- A **Table** control (`6-max` | `9-max`) in the environment rail beside depth / rake / straddle,
  default `6-max`, persisted in URL state like the other axes; `seats` joins `env`.
- The position rail renders `seatsFor(seats)` — nine chips at 9 — with the same disabled
  reasons the model returns (`positionDisabled` with `seats`), and the vs-GTO mode's "baseline is
  HU" reason on the **33** uncovered rows at nine seats (the coverage datum counts all pos × node,
  not the legal subset: I36(d) / METHODOLOGY :3405 read "3 of 24 … the other 21", so nine seats
  makes it 3 of 36 and 33. The same three HU pairs stay live: SB×rfi, BB×raise with SB raiser,
  SB×3bet).
- Everything keyed by `(pos, node)` — hand search, the skill dial's status line, the EV surface,
  top-N, Simulate's villain count — follows the rail. No new colour mode, no new panel.
- The Method view gains a **Table size** section: the ladder, the structural functions' outputs at
  both sizes, `constants.ladder` with its rule, anchor and derived values, the ring artifact's
  meta and the `extrapolated` census at nine seats — including R3's per-(pos, node) breakdown,
  which lives here and **not** on a new rail-chip badge (§3 R3).
- `smoke.mjs` gains one row: toggling the table size **re-paints inside the ON-default morph
  budget (16 ms p95)**, measured on the shipped page; the three existing morph rows stay pinned at
  8 / 16 / 4 ms. `browsers.mjs` exercises the toggle in Firefox and WebKit.

### 2.7 Byte budgets — where every new byte goes

The rule: **9-max-only bytes live in the ring block; shared bytes are the only ones allowed to
touch an existing ceiling, and each such touch is a paid raise under D6's from-above clause.**

| Ceiling (lite unless marked) | Now | What v4 adds | Disposition |
|---|---|---|---|
| `blocks.ring` (page, new) | — | Table control markup, rail 9-seat branch, Method-view section, ring accessor, the 9-max-only census readout | new cap at measured + 5 %, whole-KB. D6's from-above clause covers it **automatically**: `pageCeilingProblems` iterates `[...BLOCKS, ...Object.keys(budgets.blocks).filter(n => !BLOCKS.includes(n))]` (`gates/data.mjs:255`), so the cap is bounded at `ceil(measured × 1.05)` with no gate edit. Asserted by D13 |
| `ring` (artifact budget, **both variants**, new) | — | `data/ring.json` injected as its own build region on the `eq` precedent — a top-level `ring: N * 1024` in `VARIANTS.lite.budgets` and `VARIANTS.full.budgets` (`variant.mjs:128–129`, `:195–196`); **123 × 12 = 1,476 numbers**, est. 7–8 KB minified from the shipped columns' 4.9 B/number | new cap at measured + 5 %, whole-KB, pinned from above by **D12(d)** — D6's from-above clause explicitly excludes `eq` and its own `model.json` sub-budgets ("Still open from above: eq … and this gate's own model.json sub-budgets"), so this pin is D12's to make |
| `blocks.skill` | 3.4 / 4 KB (~560 B) | nothing — the 12 new pairs' exception entries ship inside `@block:ring` | **not raised** |
| `core` (`model.json`'s 120 KB sub-budget, `gates/data.mjs:590`) | 116.0 / 120 KB | `constants.ladder` only — a few hundred bytes; `cells` byte-identical and the ring is **not** a `model.json` sub-budget | **not raised**: the ladder constants land in the existing headroom, and `BUD`, `BUD.total`, `CORE_BUDGET` and `core` at `gates/data.mjs:584–592` need no edit |
| `modelCode` | 52.1 / 54 KB | the ladder functions replace the tables; `constants.ladder` | shrink-first (the tables and both offenders go); a raise, if measured necessary, is paid at `cap ≤ ceil(measured × 1.08)` and its D6 cite re-pinned |
| `appCore` | 359.3 / 360 KB | the rail's `seatsFor` call, `env.seats` plumbing — shared code | shrink-first (the shell's duplicate `POSITIONS` / `NNB` / `NBL` / `legalPos` go, ~0.4 KB); a raise, if measured necessary, is paid at `cap ≤ ceil(measured × 1.05)`, documented in `variant.mjs`'s `budgetSource`, re-pinned |
| `app` | 393.7 / 398 KB | = `appCore + Σcaps` by the equality pin (`test/variant.test.mjs:280` lite, `:285` full; whole-KB caps at `:288`) | raised **exactly** by the `blocks.ring` cap (+ any appCore raise), **in both variants** — the raise is mechanical, not a judgement call; the cap-sum rule, working as in P5 |
| `total` (lite) | 583.9 / 600 KB | ring block + ring data | expected to fit; a raise is paid the same way |
| `total` (**full**) | **653.7 / 660 KB** | ring block + ring data | **6,478 B of headroom — a raise is EXPECTED.** Paid at `cap ≤ ceil(measured × 1.05)` under D6's from-above clause (its bound today is 687 K), documented in `variant.mjs`'s `budgetSource` with its shrink-first sentence |

> **Measured (stage S3).** Four rows moved, and the table got two of them wrong.
> **FALSIFIED — lite's `total` did NOT fit.** §2.7 predicted it would; measured, the ring block plus
> an 18,620 B injected payload takes lite to **633,260 B = 618.4 K against 600 K**, over by 18.2 K,
> and it is paid at **625 K** (measured + 1.10 %, bound 650 K). The prediction is kept as written.
> **FALSIFIED — `appCore` was raised.** §2.7 said shrink-first would pay for it (~0.4 KB from the
> shell's duplicate tables). Measured, S1's own rewrite of those tables cost **+0.2 K net** rather
> than returning 0.4 K, and after **1,257 B** of shrink actually recovered across four lanes
> (S3 708 B, lane U 308 B, lane R 241 B) the row still reads 369,147 B lite / 369,297 B full against
> 368,640 — over by 507 B and 657 B. Paid at **361 K**, measured + 0.1 % against a +5 % bound of
> 379 K: the smallest whole-KB step that exists.
> **CONFIRMED — the full `total` raise the table expected**, 660 -> **695 K** (687.9 K measured,
> +1.03 %, bound 723 K); and **CONFIRMED — `modelCode` was NOT raised again**: five policy deltas
> took it to 57,175 B, 169 B under the 56 K S1 had already paid for.
> `app` follows the cap-sum equality mechanically: 361 + (11+12+4+5+6+11 = 49) = **410 K**, in both
> variants. New caps: `blocks.ring` **11 K** (measured 10,846 B — one whole-KB step *under* its own
> 12 K bound) and the artifact row `ring` **20 K** (18,620 B injected, D12(d)'s pin). Not raised, as
> the table promised: `blocks.skill`, `core`, `eq`, and every pre-existing block cap.

> **Measured (stage S5).** One more row moved, and it is the *documents'* raise rather than the
> feature's. **`modelCode` 56 K -> 57 K**, paid for METHODOLOGY limitation 20 shipping as DATA:
> `constants.limitations` gains its third entry so the Method view renders the admission from
> `model.constants` the way it renders 16 and 17, instead of transcribing it into the shell — the
> idiom gates I41/I42 byte-compare against the document. Measured: the entry is **267 B** of
> minified `policy.mjs`, taking the row **57,198 B -> 57,465 B** against the 57,344 B S1 paid for,
> over by **121 B**; 57 K = 58,368 B is measured **+1.57 %** against D6's calibrated bound of 62 K.
> SHRINK-FIRST, measured in bytes, and it did not pay: joining the note's two source chunks returns
> 3 B, trimming `of` to a bare phrase 6 B, and dropping `flagsItExplains` 38 B of *model payload*
> and none of `modelCode` — **47 B against a 121 B overrun**, so the raise is taken rather than the
> admission trimmed. The same entry costs `data/model.json` **263 B**, which is **not** a raise:
> it is spent out of `metaCore`'s own 275 B of headroom, leaving **13,300 B of 13,312** — twelve
> bytes, now the tightest number in the payload, and recorded in METHODOLOGY §9.11 as the reason
> the next constant needs the reserved sub-budget idiom rather than that gap. Every other ceiling
> re-measured after the document edits and none moved: lite `total` **633,856 B = 619.0 K / 625 K**,
> full `total` **705,298 B = 688.8 K / 695 K**, `app` 405.6 K / 410 K, `appCore` 360.5 K / 361 K,
> `blocks.ring` 10.6 K / 11 K, `ring` 18.2 K / 20 K.

The full variant's `eq` (73 KB) and every solver / equilibrium ceiling are untouched; **its `total`
is not** — §2.7's other rows are lite readings that hold for full to within 0.2 K (`app` 393.9,
`core` 359.4), but full's `total` has an order of magnitude less headroom than lite's and is the
row this plan expects to pay for. The D6
literal-line cites in `scripts/gates/data.mjs` **will** move (METHODOLOGY grows; `variant.mjs`'s
`budgetSource` grows) — the re-pin is a named step in stage S5, and the run is red until it is
done. README's ~20 ungated METHODOLOGY line cites are re-pointed in the same step.

### 2.8 What does not change

`data/checkdown-matrix.json`, `data/equilibrium.json`, `baselineTiers`, `calibration` (verdict
`fail`, I46 green), `evPrimary` (false), the three legacy fixtures, `orderHash`,
`constants.solver`, `constants.evCut`, `constants.skill`'s numbers, the morph budgets, the
dependency list.

What **does** change and is easy to miss, so it is listed here too: the **full** variant's `total`
ceiling (§2.7 — a raise is expected, not a contingency), the `app` ceiling in both variants (by
the cap-sum equality), and `data/model.json` by exactly one addition, `constants.ladder`.

---

## 3. Not decidable pre-measurement — encoded as rules

- **R1 — `baseR` rule** (§2.3): flat vs step, decided by I51 under both; ties go to flat.

> **Measured (stage S4).** The tie was not broken by measurement and **could not be**: the two
> candidate rules are indistinguishable on every gated surface (§2.3's annotation has the readings —
> `'step'` reproduces I49's 3.2 M frozen tiers byte-identically), so "ties go to flat" is true and
> vacuous. Flat still ships, for the reason the rule gives — fewer new numbers, no claim the model
> cannot back — but it ships as a **preference**, badged `estimate` beside `ladder.derived` and
> named in `ladder.flag`. The rejected alternative's readings are recorded as **identical on every
> gated surface**, which is the honest form of "both passed".

> **Measured (stage S5) — the two rules' readings, side by side, because "both passed" is only
> honest with the numbers under it.** Along `UTG..BTN` the candidates are `flat`
> **0.97 0.97 0.97 0.97 0.99 1.02 1.06** and `step` **0.91 0.93 0.95 0.97 0.99 1.02 1.06**; `step`
> is derived too (`baseR(LJ) + (baseR(LJ) - baseR(HJ))·k`, the ladder's own first step continued),
> so neither rule types a seventh number. I51(a)'s clauses under each: monotonicity **0 violations
> / 0**, the score surface **0 / 0**, the width surface at d = 40 and d = 250 **0 / 0**, and the
> rfi nesting clause **0 of 4,752** under both — which it cannot discriminate on, because the
> post-pass makes nesting hold by construction. `step` also reproduces I49's **3,214,728** frozen
> tiers byte-identically. `baseRaise` is rule-independent: `0.16 · 0.77^k` gives
> **0.1232 · 0.094864 · 0.07304528**, strictly increasing along the ladder under either.

- **R2 — the ring's wall-time budget**: pre-registered **300 s**, derived in §2.4 from
  `2 × 113 × 9/7 ≈ 291 s` and rounded up. If the measured two-seed run exceeds it, the executor
  **halves the lattice trials for the ring only** (`se.latt` doubles for the `N = 8, 9` columns —
  recorded in `ring.meta.se`, badged on the surfaces that read it) rather than dropping a seed;
  if it still exceeds 300 s, that is a blocker, not a silent widening. The halving clause bites
  above the derivation's own value, never at it.

> **Amended by the owner (2026-09-08, after run 1).** R2's blocker clause fired exactly as
> written: the measured two-seed wall was 1,213.6 s, the halving remedy yields ≈ 622 s, and no
> stage widened anything. The owner's adjudication, recorded here in the plan before the relaunch
> and not through any stage's prose: the 300 s pre-registration rested on a cost model §2.4's
> amendment records as falsified, so the budget is **re-derived, not chosen**, as
> `2 × (12 × 1.16 + 101 × 6.2) ≈ 1,280 s` and pre-registered at **1,280 s** (four workers). The
> rest of R2 stands unchanged with 1,280 s in place of 300 s: the halving clause bites only above
> the derivation's own value, never at it; a measured run still above the budget after halving is
> a blocker, not a silent widening; a seed is never dropped. `ring.meta.wallBudget` carries 1,280
> and D12(e) judges against it; METHODOLOGY records the falsified derivation beside the amended
> one. The D12(b)/(c) agreement band is **not** amended: run 1's treatment stands — the reading
> is reported against the pre-registered 2.0 marked falsified, and the clauses assert bias,
> spread, the 5·se per-cell bound and `eq[N = 9] ≤ eq[N = 8]` at zero tolerance.
- **R3 — the `extrapolated` census**: at nine seats the clamp is 9. The share of settings with
  `raw > 9` is measured and recorded per (pos, node); if any pair is clamped at more than half its
  66 VPIP points, that reading goes into the Method view's Table size section (§2.6) and into
  METHODOLOGY limitation 20. **No new rail-chip badge ships**: there is no per-(pos, node)
  `extrapolated` chip today — the only surfacing is the N_eff readout for the current setting
  (`src/shell.html:4405–4406`, `class="extrap"` / `badge-extrap`, the `↑` at `:4379`, the Method
  view row at `:5541`, the live-UI assertion at `:8956`) — and that readout **is** the badge.
  Building a chip badge would put new markup and CSS into `appCore`, §2.7's tightest row; any
  bytes a 9-max-only readout needs land in `@block:ring` instead. `NMAX = 10+` is out.

> **Measured (stage S4).** The threshold existed only as **prose** (`policy.mjs:437`): nothing in
> the tree computed it, so a future pair crossing the line would have tripped no gate, no test and
> no page, and the per-pair record it is defined over — `constants.ladder.census.byPair` — was not
> recounted either (I52(c) checked `domain`, `clamped` and `clamp` and stopped; three refuters of
> three replaced `byPair` wholesale with fabricated maps and I52 stayed green). Both are repaired
> in I52(c): the breakdown is recounted in **both directions**, and the half-of-66 rule is code,
> scored on **distinct VPIP points** — the quantity R3 names, which `byPair`'s per-setting counts
> overstate (a pair clamped at one VPIP under four limper counts and both straddle states scores 8
> settings and 1 point). Today's reading, printed by the gate every run: worst pair **11 of 66**
> VPIP points against the 33 that would escalate, so the clause does not fire; the day one crosses,
> the escalation must be RECORDED in `census.escalated` before the gate goes green again. The
> no-new-rail-chip decision stands unrefuted — the `N_eff` readout is a live badge and the Method
> view's census is a live sweep, not a printed copy. **METHODOLOGY limitation 20 does not exist
> yet** (the numbered list ends at 19), which is §6's "written down" leg and stage S5's work.

> **Measured (stage S5) — the census itself, and limitation 20 now exists.** At nine seats the
> domain is **6,336** UI-reachable settings (48 legal rows × straddle {off, on} × 66 VPIP; the
> work-order's predicted 5,676 is wrong because `limps` and `raise` have **8** legal seats at nine,
> not 7), the clamp is **9**, and **19 settings clamp = 0.30 %** — worst raw `N_eff` **9.96** at
> limps / UTG+1 / VPIP 90 / four limpers / straddled. Only two pairs clamp at all: `UTG1|limps`
> (14 settings, 11 of 66 VPIP points) and `UTG2|limps` (5, 5 of 66), so R3's escalation clause does
> not fire and `census.escalated` stays empty. Beside it, I48(c)'s six-seat re-derivation: **3,960**
> settings, clamp 7, **47 clamped = 1.19 %** — the integer behind the recorded percentage,
> reproduced exactly. **So nine seats MOVES the clamp rather than lifting it, and the clamped share
> FALLS.** METHODOLOGY limitation 20 is written (the numbered list now ends at 20), ships in
> `constants.limitations` and renders in the Method view from the shipped data like limitations 16
> and 17 — and it cost the one byte ceiling this stage had to move (§2.7's annotation).
- **R4 — the sub-ladder identity per node** (I50): the plan predicts rfi exact and the other
  three nodes exact **except** where a seats-in-front term enters. The executor enumerates the
  terms by reading the code, not by assumption, and I50's clause list is written from that
  enumeration before the fixture is frozen.
- **R5 — skill exceptions at the 12 new pairs**: the pairs are `UTG|rfi`, `UTG|3bet`,
  `UTG1|{rfi,limps,raise,3bet}`, `UTG2|{rfi,limps,raise,3bet}`, `LJ|limps` and `LJ|raise` — the
  ones `positionDisabled(…, 9)` newly makes legal, **not** "three new seats × 4 nodes"; the two
  `LJ` pairs are new because the first-seat exclusion moves forward, and a procedure keyed only to
  the new seat names would silently skip them. Measured by the procedure that produced
  `WIDTH_ENDPOINT_EXCEPTIONS` / `WIDTH_INTERIOR_EXCEPTIONS` (METHODOLOGY §3.5), keyed by
  `(seats, pos, node)`. If the procedure is not reproducible from the docs, that is a finding
  (recorded), the new pairs ship **without** exceptions, and I38's reach scan is extended to say
  so — never a hand-typed exception list.
- **R6 — shrink-first before any raise** (§2.7): every raise carries the measured shrink attempt
  in its `budgetSource` sentence, in bytes.

---

## 4. New constants and anchors

| Constant | Value | Anchor | Bounded by |
|---|---|---|---|
| `constants.ladder.seats` | `[6, 9]` | scope decision §0.1 | I48, I49 |
| `constants.ladder.ladder9` | the nine keys | naming decision §2.1 | I51(c) |
| `constants.ladder.earlyStep` | `0.77` — a **second constant with the same value** as `straddle.seat`, which keeps its own value and gains `derivedFrom: 'ladder.earlyStep'`; not a live reference (§2.3) | `1/√(1.250·1.350)` from `baseRaise`'s UTG→HJ, HJ→CO steps — existing, METHODOLOGY §5.3 | I51(a): monotone ladder **and** the equality `straddle.seat === ladder.earlyStep` on the shipped constants, so they cannot drift (I26's perturbation exempt by name); red team |
| `constants.ladder.baseRaiseRule` | `'geometric'` | §2.3 | I51(a) |
| `constants.ladder.baseRRule` | `'flat'` or `'step'` per R1 | §2.3 | I51(a) |
| `constants.ladder.derived` | the six derived numbers, `kind: 'estimate'` | printed beside the rule | Method view badge; red team |
| `constants.nMax` (existing) | 7 | unchanged — describes `cells` | I52 |
| `ring.meta.nMax` | 9 | §2.4 | D12, I52 |
| `ring.meta.trials`, `.se`, `.seeds` | as `generate-data` (or R2's halving, recorded) | §2.4 | D12(b), D12(c) |
| `ring.meta.wallBudget` | ~~**300 s**~~ → **1,280 s** (owner amendment, after run 1) | R2 as amended: `2 × (12 × 1.16 + 101 × 6.2) ≈ 1,280 s` from the measured per-kernel cost model (§2.4's amendment); the original `2 × 113 s × 9/7 ≈ 291 s` derivation is falsified and kept as written | D12(e) |
| `blocks.ring` (page block) | measured + 5 %, whole KB | D6's from-above clause applies **automatically** — `pageCeilingProblems` iterates `budgets.blocks` (`gates/data.mjs:255`), so no gate edit | D13 |
| `ring` (artifact budget, both variants) | measured + 5 %, whole KB | **D12(d)** — D6's from-above clause excludes `eq` and the `model.json` sub-budgets, so this pin is D12's | D12(d) |

`baseRaise` and `baseR` keep their six legacy values **bit-for-bit**; the nine-seat objects are
built by `ladderConstants(seats)` from the legacy six plus the rule. There is no seventh typed
number anywhere.

> **Measured (stage S4) — the red team's dispositions, one row at a time.**
> Six of the tally's keys came back **majority-unanchored**; every one of them now ships gated +
> flagged + badged, or has the bound it was claimed to have. Nothing was re-anchored by invention
> and no tolerance was widened.
>
> | Row | Verdict | Disposition |
> |---|---|---|
> | `ladder.baseRRule` (2 keys, 6/6 unanchorable) | **flagged** | the "decided by measurement" anchor is falsified (§2.3, §3 R1). Named in `ladder.flag`, badged `UNANCHORED['ladder.baseRRule']`, bounded by I51(a) to the two rules R1 admits, badge asserted by I51(a) |
> | R3's census thresholds (3/3) | **flagged → now gated** | the half-of-66 rule and the per-pair record are both code in I52(c); METHODOLOGY limitation 20 is S5's |
> | `ladder.seats` (2/3) | **anchored (a scope decision, now binding)** | `[6, 7, 9]` shipped green: nothing asserted the set. I51(a) requires every declared size to have a MEASURED width-exception record, and `gates/skill.mjs` reads the set instead of two literals, so `widthProblems`'s refusal is reachable from the gate |
> | `ladder.anchor` / `ladder.flag` (2/3) | **flagged, and now content-asserted** | both were length-checked only: same-length sentences saying the opposite of the truth shipped 69/69 green and rendered verbatim in the Method view. I51(a) now composes the anchor's arithmetic from the live `baseRaise` table and requires `flag` to name `baseRaise`, `baseR`, `baseRRule`, `I51` and `estimate` |
> | `RING_CEILING_FACTOR` (3/3) | **anchored** | it was a second, uncited copy of 1.05 in a file outside the generator hash: moved to 1.60 every gate stayed green. It is now `CEILING_MARGINS.blocks.factor` — D6's own margin, held to its quoted lines by `citationProblems` every run |
> | `ring.meta.model.hash` vs `.orderHash` (3/3, filed as a FINDING) | **recorded, and half of it now gated** | both fields were inert inside verify. `meta.model.orderHash` is now asserted against `data/model.json`'s own `meta.orderHash` (D12(c)) — the narrow input the villain pools are built from. The whole-file `hash` is deliberately **not** asserted: it moves on any §0.4-authorised stamp and would force a ~21-minute regeneration that changes no measured column. Narrowing the stamp is S5's documentation and a later run's generator change |
>
> Nine further claims were shown to assert less than they said; the clauses that could carry the
> repair were written (I51(a)'s composed rule and published-block comparison, I52(c)'s recount,
> D12's se-vs-trials, summary-vs-record and `orderHash` clauses, D13's cap-sum / no-raise /
> shrink-first clauses), and the rest are recorded in `docs/refutations/V4.md`.

---

## 5. The gate catalog — id, claim, how it FAILS

### 5.1 Dispositions of the existing 62

Unchanged in claim; **four change in domain** and must be re-read at nine seats:

- **I15** (positional nesting) is evaluated at both sizes by I51 rather than extended in place.
- **I26** keeps perturbing `straddle.seat` **alone**. `ladder.earlyStep` is a second constant with
  the same value, not a live reference (§2.3), so the perturbation does not flow into the opening
  ladder and I26 cannot fight I51(a); what I26 gains is I51(a)'s equality assertion standing over
  the unperturbed pair.
- **I38(e)**'s reach scan must see the 12 new pairs (R5's enumeration, `LJ|limps` / `LJ|raise`
  included).
- **I36** changes domain because its (b) clause reads `scripts/lib/equilibrium.mjs:710`'s literal
  `['UTG','HJ','CO','BTN']` in `nestingReadiness` — one of the five re-typed `NEST_CHAIN` literals
  §2.1 replaces with an import. It becomes `nestChain('rfi', 6)` and stays **six-seat-scoped**, so
  the armed clause ("fails the day a payload covers two seats of the UTG/HJ/CO/BTN chain") keeps
  exactly the meaning it has today. I36(d)'s coverage datum is re-read at nine seats as 3 of 36,
  33 uncovered (§2.6).

D6 gains the `blocks.ring` row (automatically bounded, §2.7) and its cites move (§2.7); the `ring`
artifact budget is D12(d)'s to pin, as `eq`'s floor is D9's. The `app` raise that follows the
`blocks.ring` cap is mechanical and lands in **both** variants (`test/variant.test.mjs:280`,
`:285` assert `app === appCore + Σcaps` as an equality; `:288` requires whole-KB caps).
`freeze-tiers.mjs:150`'s kind selector gains a branch (§2.5). D11 (dual determinism) runs on the
new shell. No gate is deleted; no tolerance moves.

### 5.2 New gates

**I48 — seat-axis inertness.** (a) The three legacy fixtures compare clean **and are
byte-unchanged across every commit in the run's range** (`git diff <base>..HEAD --
data/tiers-*.fixture.txt` empty) — that is what "no `--force`" means operationally; `--force` is a
CLI flag and never appears in a commit message, so a `git log` grep is not the checkable artifact.
Operationally, `<base>` is **the HEAD sha S0 recorded**, threaded through to the close, and the
claim is a claim about the *shape* of the diff: `git diff --stat <base>..HEAD -- data/` shows the
three legacy fixtures **unmodified** and `data/tiers-9max.fixture.txt` **added**. Added-not-modified
is what "no `--force`" looks like from the outside; a legacy fixture showing as modified fails the
gate whatever the log says.
(b) Tripwire: with `data/ring.json` zeroed on a copy, the full seats-6 surface (every fixture
setting) is byte-identical to the real build's. (c) `nMax(6) === 7`, and the seats-6
`extrapolated` count over V3-BRIEF :211's own domain — the 3,960 UI-reachable settings, whose
enumeration the executor re-derives and records — is the **integer** behind the recorded 1.19 %
(47 of 3,960 by that percentage; the gate asserts the recounted integer, because a percentage is
not a gate). *Fails* on any diff, any fixture byte moving, any read of the ring at six seats.

**I49 — the 9-max fixture.** `data/tiers-9max.fixture.txt` reproduces on every run
(`freeze-tiers.mjs --seats9 --check` empty), 26,136 settings or the measured count with the
prediction annotated. *Fails* on any tier move — the same claim I32 makes for six seats.

**I50 — the sub-ladder identity.** For every (node, VPIP, lane, cell) where R4's enumeration
says the inputs coincide, tier(9-max `LJ..BB`) === tier(6-max `UTG..BB`) — over **every shared
seat where the pair is legal at both sizes**, which is not "all six" at any node: at `rfi` `BB` is
disabled at both, leaving five (`LJ..SB`); at `limps` and `raise` the first-seat exclusion moves
forward, so `LJ` has no six-seat counterpart and five remain (`HJ..BB`); at `3bet` all six.
`LJ|limps` and `LJ|raise` are I49's business, not I50's. Per-node clauses are written from the
enumeration, and **rfi is pre-registered as containment, not equality, with the direction
stated**: the nesting post-pass at `policy.mjs:2104–2110` unions every seat *in front* of `pos`
into `pos`'s aggressive set (the shell mirrors it at `:3200–3212`, `a[i] |= b[i]`; METHODOLOGY
limitation 10), so at nine seats `LJ`, `HJ`, `CO` and `BTN` each gain three additional unions
(`LJ` moves from `ci === 0` — no union at all — to `ci === 3`), while `SB` is outside the rfi
chain (`ci === -1`) and **is** pre-registered exact. The prediction is therefore
`aggressive(9-max, seat) ⊇ aggressive(6-max, seat)` at rfi for those four, with equality
**measured**, not assumed; the excess is enumerated cell-by-cell and the count of unioned cells
per shared seat is recorded in the freeze's sub-ladder diff (§2.5). Each excess cell must be
explained by a front seat's own `N_eff` — `N_NB[UTG@9] = 6` vs `N_NB[LJ@9] = 3` changes
`realization`, `nuMin(N)` and whether the `N >= nutGate[2]` demotion fires — and the fact that a
unioned cell **bypasses `pos`'s own nut gate entirely** (the demotion is skipped for cells already
in `active`) is a finding to record in METHODOLOGY, not to launder. A **strict** superset at any
of the four is an expected outcome, not a failure. Where a seats-in-front term makes the
*pre-nesting* width differ (`widthFor`, where `baseRaise` is strictly increasing along the
ladder), the difference must be **monotone in the term** — more seats in front never widens the
pre-nesting target; the painted set, after the union, can only grow, so the monotonicity clause is
scoped to width and stated separately from the painted set. *Fails* on a **subset** violation at
rfi, an unexplained excess cell, or a non-monotone pre-nesting width.

> **Measured (stage S3).** The clause list was written before the freeze and each shared seat sorted
> **structurally**, by its index in `nestChain(node, 9)` — the only seats-in-front term R4
> enumerated. Ten pairs pre-registered CONTAINMENT, eleven pre-registered EXACT, over 16,632
> comparisons (21 pairs x 12 lanes x 66 VPIP, none refused):
> **0 subset violations. 0 unexplained excess cells. 0 pre-nesting differences of any kind.**
> **`i50RfiExact` is FALSE, and that is the expected reading**: of the ten containment pairs, seven
> measured a STRICT SUPERSET and three measured equality. At rfi, `LJ` gains 30 excess cells,
> `HJ` 10 and `CO` 4 — **and `BTN` gains none at all**, though it sits at chain index 6 with three
> new seats in front of it: its own pre-union set already contained theirs. The eleven exact pairs
> are exact, as pre-registered.
> **REFINED, not patched: the plan predicted rfi and measured `raise`.** §5.2 wrote the containment
> clause for rfi alone. `nestChain('limps'|'raise', 9)` runs UTG+1..BTN, so HJ/CO/BTN gain three
> unions at those nodes too, and `raise` is where nine-seat nesting bites **hardest** — 1,210 of
> 2,376 shared raise settings paint a strict superset (1,638 excess cells) against rfi's 42 (44
> cells). I50 is therefore containment at **three** nodes, not one.
> **The pre-nesting clause holds as EQUALITY**, which is stronger than the monotone wording it was
> written to: 0 differences over 16,632 comparisons. R4's enumeration — one seats-in-front term in
> the whole pipeline — is confirmed by measurement rather than by reading.
> **The nut-gate bypass is REAL and NOT load-bearing here**, recorded rather than laundered: `solve`
> skips the `N >= nutGate[2]` demotion for cells already in `active`, so a unioned cell bypasses
> `pos`'s own gate — and **0 of the 1,046** excess cells S1 attributed are cells that gate would
> have demoted. METHODOLOGY limitation 10 carries this.

**I51 — the ladder.** (a) `baseRaise` strictly increasing along the non-blind ladder at both
sizes; `straddle.seat === ladder.earlyStep` on the shipped constants (the two-constants-one-anchor
pin of §2.3 — I26's perturbation is exempt by name); `baseR` non-increasing toward the front under
whichever R1 rule ships, read on **both** the score and the width surface (`depthWidthFactor`,
§2.3), i.e. in the d = 40 and d = 250 lanes as well as at the reference depth; nesting `UTG ⊂
UTG1 ⊂ UTG2 ⊂ LJ ⊂ HJ ⊂ CO ⊂ BTN` at rfi at every VPIP and lane. (b) The two rewritten
offenders reproduce their literal seat sets at six seats and name `{BTN,SB,BB}` / `{CO,BTN}` at
nine. (c) **No new seat-name literal**: a lexical scan of `scripts/`, `src/`, `test/` finds the
nine keys only inside `LADDER9`, `seatsFor`, fixtures and the display map. *Fails* on any
violation; (c) fails on the first stray literal, by file:line.

**I52 — the ring consumer.** At nine seats `nMax === 9`, every `N_eff ∈ (7, 9]` reads the ring
columns **through `eqAtSeats`** (tripwire: perturb `data/ring.json` → those settings move, and
only those — written against the accessor, never against `cells`, which stays byte-identical);
`meta.nMax` and the page's `NMAX` are still 7 while `SIM_NMAX` is 9 (§2.4); the `extrapolated`
census at nine seats is recorded in `constants.ladder.census` and matches a live recount. *Fails*
on a stale census, a setting above 7 that does not move under perturbation, or a `cells` read at
`N > 7`.

**D12 — the ring artifact.** (a) `generate-ring.mjs --check` byte-identical; (b) meta complete
(seeds, trials, se, generatorHash, contentHash) and the two seeds' `N = 8, 9` columns agree within
**`2 · se.cell` = 0.32 equity points** on every cell — the same band (c) uses and the quantity
`meta.se.cell` already names; `solver.twoSeedTolPot` (0.15 % of the preflop pot, `cfr.mjs:127`,
`:202`) is the *solver's* two-seed tolerance on solved values and has no meaning applied to an
equity column; (c) **the unshipped `N = 1..7` prefix agrees with `cells[*].eq[0..6]` within
`2 · se.cell` on every cell** (the free reproduction); (d) the **`ring` artifact budget** — the
top-level per-variant row, not a `model.json` sub-budget — pinned from above at
`ceil(measured × 1.05)`, whole-KB, in both variants, because D6's from-above clause explicitly
excludes it; (e) the measured wall time ≤ `ring.meta.wallBudget` (300 s as written; **1,280 s** after the owner's R2 amendment). *Fails* closed on each.

> **Measured (stage S5, from lane R run 2).** **(c) IS A REAL FALSIFIER AND IT FALSIFIED THE BAND
> IT WAS GIVEN.** The unshipped `N = 1..7` prefix does reproduce the shipped layer, but not to
> `2 · se.cell = 0.32` pt: the worst per-cell prefix delta is **0.8488 pt = 5.31 · se.cell** (3.75 σ
> of the artifact's own `seDiff = 0.2263`), and (b)'s worst two-seed delta is **0.8409 pt =
> 5.26 · se.cell** (3.72 σ). **The pre-registered 2·se band is reported, never asserted**, exactly
> as the owner's R2 amendment left it. What IS asserted and passes: bias **0.436 σ** (prefix) and
> **0.129 σ** (two-seed) against 2, spread **0.922×** and **0.847×** against 2, and the **5 σ**
> outlier line on all 123 cells. The count past the pre-registered band — 88 of 861 prefix numbers
> and 153 of 1,476 two-seed — is **fewer than Gaussian noise at `seDiff` alone predicts** (135 and
> 232), which says the band was too tight rather than the measurement too noisy. Two zero-tolerance
> monotonicity readings ride with it, and the first is a cross-artifact check neither file can make
> alone: `eq(N = 8) ≤ eq(N = 7)` on every cell (worst **−0.700** pt) and `eq(N = 9) ≤ eq(N = 8)` on
> every cell (worst **−0.500**). §2.4's stated MECHANISM for (c)'s independence is also falsified,
> and the conclusion survives it: at the same seeds, widening `runMulti` from 7 to 9 leaves
> `eq[0..4]` **bit-identical** rather than re-streaming everything, so what makes the prefix an
> independent reproduction is the ring's own two named seeds, not the draw width. (d) pins the
> artifact row at 20 K over a measured 18,620 B, and (e) is green at 1,255.4 s against 1,280.


**D13 — the ring block.** `@block:ring` present in **both** variants; its cap bounded from above
by D6's blocks clause (`pageCeilingProblems` iterates `[...BLOCKS, ...Object.keys(budgets.blocks)…]`
at `gates/data.mjs:255`, so a new `blocks.ring` cap is bounded at `ceil(measured × 1.05)` with no
gate edit); the equality pin `app === appCore + Σcaps` holding with `ring` in the sum in both
variants (`test/variant.test.mjs:280`, `:285`; whole-KB caps at `:288`); `blocks.skill` (page) and
`core` — `model.json`'s 120 KB sub-budget at `gates/data.mjs:590`, **not** the page ceiling D6
also prints as `core`, which is `appCore` — **not raised** (their caps equal `1d988f5`'s); and any
`appCore`, `modelCode` or full-`total` raise carrying its shrink-first measurement in
`budgetSource`. *Fails* on a missing block, a silent raise, or a raise without its shrink sentence.

> **Measured (stage S4).** As shipped at S3, D13 asserted the FIRST clause of that paragraph and no
> other: its body checked that `blocks.ring` and `ring` existed in both variants and that
> `@block:ring` was in both artifacts. Two refuters measured the rest. Deleting every shrink-first
> sentence from both `budgetSource` strings left D13 and D6 green (three regexes in
> `test/variant.test.mjs` were the only red, and they pin *phrases*, so falsifying the byte figures
> inside them shipped green too); raising `blocks.skill` 4K → 5K was caught by D6's from-above
> clause and not by D13; raising `model.json`'s `core` sub-budget 120K → 130K left **verify 69/69
> green** with D6 printing "of which core 116.1K/130K", caught only by a regex in a test. The three
> missing clauses are now D13's own: the cap-sum equality with `ring` in the sum in both variants;
> `blocks.skill` and `model.json`'s `core` / `metaCore` / `skill` sub-budgets read from D6's own
> table (`MODEL_SUB_BUDGETS`, moved to module scope at S4 so a gate outside that family can assert
> it); and every ceiling ABOVE its v3 release value (`1d988f5`) required to carry its raise **in
> arrow form** — `-> <new cap>K` — beside a `SHRINK-FIRST, MEASURED IN BYTES` sentence, so a raise a
> later run makes without touching the note fails here rather than in a phrase test. Armed:
> `total` 625K → 650K, `blocks.skill` 4K → 5K, `appCore` 361K → 365K and a deleted shrink sentence
> each fail by name (`test/gates-ring.test.mjs`). What D13 still cannot do is verify that a recorded
> shrink actually happened — that remains documentary, and §5.2 now says so rather than implying a
> measurement.

**Registration, which is a three-line edit per id and is itself gated.** For each of I48–I52, D12
and D13: add an entry to `scripts/gates/reserved.mjs` with `status: 'live'`, add the id to its
family's `ids`, and **append** it to `EXPECTED_IDS` (`scripts/gates/index.mjs:91`, a frozen ordered
literal) — appended, never interleaved, because `index.mjs:161` compares the declared sequence and
the report order is a thing reviewers diff. The import-time guards in `index.mjs` and
`test/gates-reserved.test.mjs` fail loudly on any two of the three. Lane F owns all seven
registrations (§7.2). **`I48` is claimed deliberately**: `test/payoff-model.test.mjs:24` names it
as an id it declined to invent ("Inventing I48 here would be exactly what
`scripts/gates/reserved.mjs` was written to prevent") — that comment forbids choosing an id
*after* the feature; §5 reserves it *before* one, which is the distinction `reserved.mjs` draws,
and this sentence is the written record that the divergence is intentional.

### 5.3 Adversarial verification duty

Every constant in §4 and every rule in §3 is attacked by three independent refuters (§7.4);
majority rules; memos committed verbatim to `docs/refutations/V4.md`. A majority-unanchored
constant ships gated + flagged + badged or is cut — the P3/P4 precedent.

---

## 6. METHODOLOGY / README repairs

- METHODOLOGY: a new **§3.6 The table-size axis** (the ladder, the structural functions, the
  early-seat rules with their derived values and the red team's verdict, the ring artifact, the
  census, the sub-ladder diff from the freeze); **limitation 20** (the early-seat constants are
  extrapolated from the model's own six-seat ladder — no nine-handed corpus of any kind has
  touched them); **limitation 10** (the nesting post-pass) gains I50's nine-seat reading — the
  containment measurement and the nut-gate bypass §5.2 records; §9.11 gains the ring rows and the
  raise sentences; §10's "7-max and 9-max deferred" line rewritten to "7-max / 8-max out; 9-max
  shipped as an axis". The `SIXMAX` prose becomes table-size-neutral at **all four** sites —
  **:3406** (the entry itself), **:3425**, and the two gate rows **:3565** (I35, quoting
  `SIXMAX.reopenRule`) and **:3566** (I36, quoting `SIXMAX.reopenVerdict`) — together with
  `scripts/lib/cfr.mjs:901` and every reader of the constant in `scripts/gates/solver.mjs`. It is
  not a 14-line block.
- README: **"What v4 added"** after "What v3 added"; backlog item 16 closed with the 7/8 residue
  restated; "Regenerating the data" gains `generate-ring.mjs`; "Repo layout" corrected for the
  dual build (an open v3.1 item, repaired here because the section is being edited anyway).
- V4-PLAN: annotated in place; §10 resolutions appended.
- `package.json` version → `4.0.0-dev` at the commit.

---

## 7. Orchestration — one launch, flat context

### 7.1 The contract with the launching session

The launching session is **Fable, top-level, and does three things only**: launches the
workflow, receives its return value, and reports it. It never reads a source file, a worker's
detail file, a diff or a log. Everything below is enforced by the script, not by discipline:

- **One `Workflow` call.** `.claude/workflows/v4.js` runs S0–S6 end to end; control flow is
  deterministic JS outside the session; the session sees the launch line and one return.
- **Every `agent()` returns a schema.** Reports are structured and capped (§7.3). Anything longer
  is a **file** — `docs/spikes/V4-*.md`, `docs/refutations/V4.md`, or the run's scratchpad — and
  the return carries the path. The final return prints as **one line** (~460–900 characters) and
  **51–55 lines pretty-printed** — measured against the script, not asserted — every line of it a
  number, a boolean or a short path (§7.3).
- **`blockers` is reserved** (v3 HOUSE rule): only what must stop the commit. Findings, resolved
  trade-offs and deferred items go in `summary`.
- **The script's own Fable tier is the last Fable layer** (V3-PLAN §11): the run is launched from
  a top-level session, never from inside a sub-orchestrator; the milestone architect and the
  triage step are read-only.
- **No questions to the user mid-run.** A genuine decision only the owner can make is a blocker
  in the return; the owner adjudicates in prose; the relaunch uses `resumeFromRunId`, which
  replays completed `agent()` calls from cache so only the changed stage re-runs (the P3
  precedent, decision 13).
- **Model tiering, every call explicit** (v3 policy): `fable @ high` architect (work-orders for
  the max-effort steps, triage before the fix round; read-only); `opus @ xhigh` default worker
  (lanes, integration, refuters, docs); `opus @ max` for the ladder generalisation (S1), the
  fixture freeze + identity proof (S3) and the fix round; `sonnet @ medium` for the precheck and
  the verify agents; `haiku` for the commit.

### 7.2 Stages, agents, ownership

| Stage | Who | Where | Owns (single writer) | Returns |
|---|---|---|---|---|
| **S0 precheck** | sonnet | main tree, read-only | — | `{green, head, gateCount, fixturesPresent, forceInLog:false}`; aborts the run on red |
| **S1 ladder generalisation** | fable architect (work-order) → opus max | main tree, serial, uncommitted, published as the snapshot branch `v4-s1-base` | `scripts/lib/policy.mjs`, `scripts/lib/skill.mjs` (keying only), the six re-typed literal sites, `src/shell.html:2182–2199`, `:3155–3156` (`N_EFF_MAX`) + `:3202` **only** | `docs/spikes/V4-ladder.md` (≤ 80 lines: the interface, `seatsFor`, structural functions, R1's readings, R4's enumeration, the census) + `{identityAtSix: true, modelCodeBytes, blockers}` |
| **S2 fan-out** (four lanes, isolated worktrees, parallel) | opus xhigh × 4 | worktrees branched from `v4-s1-base` | **R** ring: `generate-ring.mjs`, `lib/ring.mjs`, `data/ring.json`, the `nMax` **per-call option** through both `mc.mjs` kernels (`:82–144`, `:168–229`) and `sim-kernel.js:28`'s per-job field, keeping `@worker-slice-start…end` byte-sliceable with `test/sim-bundle.test.mjs` as the acceptance test, the ring payload handed to S1's `eqAtSeats` accessor (policy.mjs is frozen after S1, so the accessor is S1's, with a payload parameter), and three named `src/shell.html` sites — `SIM_NMAX`, `validEqArray` (`:1318`) and the `p.nMax` check (`:1407`) — line-disjoint from S1's three ranges and U's remainder (§2.4), `gates/ring-artifact.mjs` D12 clauses, tests — **U** UI: `src/shell.html` (rest of the file), `smoke.mjs`, `browsers.mjs` — **F** fixtures/gates: `freeze-tiers.mjs` kind + the `:150` selector branch, `lib/tier-fixture-9max.mjs`, `gates/ring.mjs` I48–I52 clauses, **and the id registrations** (`gates/reserved.mjs` entries, family `ids`, the `EXPECTED_IDS` append at `gates/index.mjs:91` — §5.2) (F owns the file; R appends D12 via a separate file `gates/ring-artifact.mjs`) — **K** skill/solver prose: `skill.mjs` measurement at R5's 12 pairs (`UTG\|rfi`, `UTG\|3bet`, `UTG1`×4, `UTG2`×4, `LJ\|limps`, `LJ\|raise`), `cfr.mjs` rename, `equilibrium.mjs` reasons (including `:710`'s chain literal → `nestChain('rfi', 6)`, I36's armed clause unchanged), `gates/baseline.mjs` labels | each: `{files, gateIds, measured, blockers}` ≤ 15 lines + a `docs/spikes/V4-<lane>.md` memo |
| **S3 integration + freeze** | fable work-order → opus max | main tree, serial | merges R, U, F, K; `variant.mjs` budget rows; `build.mjs` block registration; **the fixture creation** (`freeze-tiers.mjs --seats9`); the sub-ladder diff into METHODOLOGY | `{merged, budgets:{...}, fixtureSettings, i50Clauses, blockers}` |
| **S4 red team** | opus xhigh × 3 refuters + 1 resolver | memos only (no source writes by refuters) | `docs/refutations/V4.md` | resolver: `{constants:[{name, verdict, disposition}], blockers}` |
| **S5 docs + re-pin** | opus xhigh | main tree | METHODOLOGY, README, V4-PLAN annotations, `gates/data.mjs` cites, `package.json` | `{sections, citesRepinned, blockers}` |
| **S6 close** | sonnet verify → fable triage (on red) → opus max fix (once) → sonnet re-verify → haiku commit | main tree | — | the run's return (§7.3) |

**Contention registry.** `policy.mjs`: S1 only; frozen for S2 — a lane that needs a policy
change *files it* in its return (`policyDeltas`), and S3 applies the deltas serially, then re-runs
I48 before continuing. `src/shell.html`: S1 touches its three named ranges (its §7.2 row), R its
three named sites (the lane-R row), U owns the rest; the merge is line-disjoint by construction. `scripts/gates/ring.mjs` (F) and `ring-artifact.mjs` (R)
are separate files so the two lanes never share one. `scripts/gates/index.mjs`: **lane F is the
single writer** of `EXPECTED_IDS` and of `gates/reserved.mjs`, and it registers all seven ids
(I48–I52, D12, D13) in one place — R reports `D12` in its return and F appends it. This is not a
shared append-only file: a mismatch between the declared sequence and that frozen literal makes the
runner *throw* rather than fail a gate (§5.2), so two lanes each appending is a failure mode with no
gate to catch it, and S3 has nothing to reconcile. `freeze-tiers.mjs`: F writes the kind, S3
runs it — a stop-the-world event, once. `variant.mjs` / `build.mjs`: S3 only.

**The S1→S2 hand-off.** S1's work stays uncommitted (above), but a worktree is cut from `HEAD` and
does not carry the main tree's uncommitted work — measured in the v3 P2 run — while all four lanes
need `seatsFor` and the structural functions to *exist*. So S1's last action is a snapshot ceremony
that changes nothing about its tree: `git add -A` (stage, do not commit), `SNAP=$(git stash create)`
(a commit **object** for the working tree — it touches neither HEAD, nor the index, nor the stash
list), `git branch v4-s1-base $SNAP`. Each lane's first action is to grep its own `policy.mjs` for
`seatsFor` and, if it is absent, `git merge --ff-only v4-s1-base` — a no-op in the world where the
harness does carry dirty state into worktrees, and a blocker (never a guess) if the branch is
missing. S3 folds the snapshot back before merging: if the tree is dirty and `git diff v4-s1-base`
is empty, `git reset --soft v4-s1-base` + `git commit --amend`, so the lane merges apply to a clean
tree that already shares their base; if that diff is **not** empty, S3 stops, because the snapshot
no longer describes the tree and a merge would silently drop work. That mid-run commit is merge
bookkeeping: **the boundary commit is still S6's and still single**, and nothing is pushed.

### 7.3 Schemas and caps

Every schema has `summary` (≤ 12 lines), `blockers` (array, reserved), and stage-specific
measured fields as **numbers**, never prose. The final return:

```
{ green: bool, committed: sha|null, gates: {total, fail:[]}, tests: {total, fail},
  build: {lite:{total, app, appCore, ring}, full:{total, app, appCore, ring}}, fixture: {settings, path},
  identity: {i48: bool, i50: {rfiExact: bool, clauses: n}}, ring: {wallSec, prefixAgreement},
  raises: [{ceiling, from, to, shrinkFirstBytes}], redTeam: {constants, unanchored},
  detailFiles: [paths], blockers: [] }
```

That object is the whole of what enters the launching session: 1 line compact, 51–55 pretty-printed.
Every variable-length part is **hard-capped in the script** so that a red run cannot print larger
than a green one — `gates.fail` ≤ 4, `raises` ≤ 3, `redTeam.unanchored` ≤ 3, `detailFiles` ≤ 6,
`blockers` ≤ 4 at 150 characters each, every string whitespace-collapsed and truncated. Shape won
over line count: reaching 40 pretty-printed lines would mean flattening `build` and `identity`, i.e.
renaming fields this section names. The Fable architect's work-orders and triage are written to
`docs/spikes/V4-workorders.md`, not returned.

### 7.4 The red team

Three refuters, each briefed with §3, §4 and `docs/spikes/V4-ladder.md`, each writing one memo
per constant to the scratchpad; a resolver merges them into `docs/refutations/V4.md` and applies
majority rule. Targets, in order of expected heat: `ladder.earlyStep`'s promotion (is one seat of
the *six*-seat ladder the right step for seats that ladder never contained?); the geometric rule's
output at UTG; R1's choice; the ring's two-seed tolerance and the choice of `NMAX = 9` (why not
8); D12(c)'s `2 · se` agreement band; the `extrapolated` census thresholds in R3; every raise in
§2.7 and its shrink sentence; the `LJ` naming (a rail label is a claim too).

### 7.5 Failure handling

Exactly **one fix round** per run (opus max, under Fable triage), then a blocker report. The
report names the stage, the file paths, and the decision the owner must make in one sentence
each. Relaunch with `resumeFromRunId`; unchanged stages replay from cache.

The owner's adjudication reaches the run through **one argument**, not through edited briefs:

```
Workflow({ scriptPath: ".claude/workflows/v4.js", resumeFromRunId: "<id>",
           args: { adjudication: { stage: "S3", text: "<the owner's decision, in prose>" } } })
```

The prose is appended to **that stage's prompt only** (stage ids `S0`, `S1`, `S2-R|U|F|K`, `S3`,
`S4`, `S5`, `S6`), so every earlier `agent()` call keeps a byte-identical prompt and replays from
cache — appending it to every brief would change every prompt and destroy the very cache
`resumeFromRunId` exists for. With no `args`, every prompt is unchanged. The prose **binds** the
stage that receives it and may not weaken a gate, widen a tolerance or authorise `--force`.

### 7.6 Launch

From a top-level session, on `main`, tree clean:

```
Workflow({ scriptPath: ".claude/workflows/v4.js" })
```

The script is written from §7.2–7.5 and `.claude/workflows/v3.js`'s helpers (`HOUSE`, the model
tiers, `closeMilestone`, `redTeam`, the schemas, worktree isolation) and committed beside this
plan. A `/v4` skill mirroring `/v3` is optional sugar; the launch line above is the contract.

---

## 8. Sequencing, barriers, risks

| Stage | Ends at | Depends on |
|---|---|---|
| S0 | precheck green | — |
| S1 | **B1**: `identityAtSix` proven (three fixtures clean, no diff), interface memo written | S0 |
| S2 | **B2**: four lanes returned, no blockers | B1 (every lane reads the memo) |
| S3 | **B3**: merged, built, fixture created, I48–I52 + D12/D13 green | B2 |
| S4 | **B4**: refutation record committed to the tree, dispositions applied | B3 |
| S5 | **B5**: docs written, D6 cites re-pinned, three checks green | B4 |
| S6 | commit | B5 |

**Estimated run:** ~17 agents (1 + 2 + 4 + 2 + 4 + 1 + 3), on the order of the P3 run (11
agents, 3.5 h) plus the ring generation's two-seed wall time. Zero owner interaction if green.

**Top risks.** (1) The rfi sub-ladder identity is **containment, not equality**, because the
nesting post-pass unions every seat in front into `pos` (`policy.mjs:2104–2110`): `LJ`, `HJ`, `CO`
and `BTN` each gain three unions at nine seats, so a strict superset is the expected reading and
only a subset violation fails. The risk is that the excess is *unexplained* — or that a unioned
cell bypassing `pos`'s nut gate turns out to be load-bearing — in which case I50 records the
mechanism and the plan's prediction is refined in place, not patched.

> **Measured (stage S3). The top risk did not fire, and the prediction is refined in two places.**
> Every excess cell is explained — **1,722 of 1,722**, each carried by a named front seat's own
> pre-union aggressive set — and **0 of 1,046** bypassed a nut-gate demotion that would otherwise
> have fired, so the bypass is real in the code and not load-bearing on this surface. What the risk
> statement got wrong is *where*: the bite is at `raise`, not `rfi` (1,210 strict supersets against
> 42), and `BTN` at rfi gains its three unions but **zero cells**. Both are recorded under §5.2.

(2) `appCore`'s 682 B
headroom, and the **full variant's 6,478 B of `total` headroom** (§2.7), force raises despite
shrink-first — paid and documented; the red team attacks each. (3) The ring generator overruns
300 s — R2. (4) The skill-exception procedure is not reproducible from
METHODOLOGY — R5 ships the pairs bare and records it. (5) The D6 re-pin turns the run red late —
the S5 step is named, and the failure message names the new line.

---

## 9. Explicit non-goals

7-max / 8-max; any multiway baseline or GTO label at nine seats; regenerating `cells`; any
`--force`; mobile; 5-card; the rest of the v3.1 backlog; pushing.

## 10. Open questions for the run (none block launch)

1. Does `positionDisabled` at nine seats yield exactly the predicted 33 pairs, or does a node
   carry a second structural exclusion the six-seat table never exposed? (S1 records.)
2. Is `baseRaise(LJ) = 0.16` still the right *anchor seat* for the geometric rule, given the
   straddle's `seat` was derived from the UTG→HJ and HJ→CO steps — should the rule instead
   extend from the geometric mean of the *whole* non-blind ladder? (Red team, §7.4.)
3. Does the shell's fallback path (`window.POLICY` absent) need nine seats at all, or is it
   legacy-only by definition? (U records; the plan's answer is legacy-only.)

> **Measured (stage S4) — question 2 is answered, in the shipped anchor's favour.** All three
> refuters computed the alternative: the geometric mean of the *whole* non-blind ladder is
> `(0.45/0.16)^(1/3) = 1.4057–1.4117`, i.e. `earlyStep ≈ 0.708–0.711`, which puts nine-handed UTG
> at `baseRaise ≈ 0.057` — **tighter** than the shipped 0.0730. So extending from `baseRaise(LJ)`
> with the two-early-step mean is the more conservative of the two readings, the anchor **seat** is
> the defensible half of the construction, and the CO→BTN exclusion the straddle made is inherited
> rather than re-argued. Two things the same measurements say, recorded because they cut the other
> way: the anchor is arithmetically insensitive to `baseRaise.HJ` (the product of the two steps is
> `CO/UTG` however HJ is set, so `earlyStep = sqrt(baseRaise.UTG/baseRaise.CO)` and the middle seat
> cancels — I51(a) now re-derives exactly that); and the model's own three steps **accelerate**
> toward the button (1.250, 1.350, 1.667), so extrapolating the *second-order* trend backwards
> gives 0.1478 / 0.1577 / 0.1942 — a ladder that inverts at UTG+1 and that I51(a1)'s strict-increase
> clause, a gate rather than evidence, is what rejects. Two model-anchored readings of one dataset
> differ by 2.7x; the defensible band on the model's own evidence is `earlyStep ∈ [0.711, 0.800]`,
> inside which 0.77 sits without being singled out. That is what the `estimate` badge is for.

### 10.1 Resolutions *(appended at stage S5)*

All three are answered by measurement, and each names the evidence that answered it. Nothing here
was decided by preference.

1. **Does `positionDisabled` at nine seats yield exactly the predicted 33 pairs, or does a node
   carry a second structural exclusion the six-seat table never exposed?** **RESOLVED — exactly 33,
   and there is no second exclusion.** `positionDisabled(…, 9)` refuses three of the thirty-six
   pairs and they are the same three structural facts as at six: `BB|rfi` ("BB closes the unopened
   pot by checking"), `UTG|limps` and `UTG|raise` ("no one acts before UTG"). The split is
   `{rfi 8, limps 8, raise 8, 3bet 9}`, against 21 at six. Evidence: S1's ladder generalisation
   (`docs/spikes/V4-ladder.md` §3) and lane K's independent re-derivation
   (`docs/spikes/V4-skill.md` §2), which agree on the count and disagree on nothing; the fixture
   ceremony then froze 33 × 66 × 12 = **26,136** settings with **zero refusals**, which is the same
   claim made a third time by a different route. The one thing measurement added to the prediction
   is a caution the plan did not have: "the twelve new pairs" has **two** derivations — by key and
   by ladder position — that differ by two pairs each way, so the shipped skill records cover all
   33 rather than either twelve (§2.2's annotation).

2. **Is `baseRaise(LJ) = 0.16` still the right anchor seat, or should the rule extend from the
   geometric mean of the whole non-blind ladder?** **RESOLVED in the shipped anchor's favour, and
   the reason is that it is the more conservative of two defensible readings** — see §10's
   `Measured (stage S4)` block above for the full arithmetic. The whole-ladder mean gives
   `earlyStep ≈ 0.708–0.711` and a nine-handed UTG at `baseRaise ≈ 0.057`, *tighter* than the
   shipped 0.0730; the model's own second-order trend gives a ladder that inverts at UTG+1. Two
   model-anchored readings of one dataset differ by **2.7×**, the defensible band is
   `earlyStep ∈ [0.711, 0.800]`, and 0.77 sits inside it without being singled out. Evidence: three
   independent refuters, `docs/refutations/V4.md`. The disposition is the `estimate` badge and
   METHODOLOGY limitation 20, not a re-anchoring.

3. **Does the shell's fallback path (`window.POLICY` absent) need nine seats at all, or is it
   legacy-only by definition?** **RESOLVED — legacy-only, and the answer is now structural rather
   than a decision.** The plan's answer was right for a better reason than it gave: `HAS_SEATS`
   requires an injected policy exporting `seatsFor` / `nMax` / `positionDisabled` /
   `behindNonBlind` / `rhoAtSeats` **and** `constants.ladder` in the shipped model, so without them
   the Table control never renders and `S.seats` can only be 6. The fallback's six-seat list is kept
   and marked legacy; its `aggNested` chain falls back to `POSITIONS.slice(0, NP-2)`, which is
   literal-free. Evidence: lane U's memo (`docs/spikes/V4-ui.md` §4), which also measured the
   related refusal the plan did not ask about — **328 of the 6,336** nine-seat settings (5.18 %),
   every one at the iso node, drive `N_eff` past 7, and on a build carrying no `data/ring.json` the
   accessor **fails closed by name** rather than silently proxying with a clamp.
