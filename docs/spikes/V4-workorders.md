## S1-ladder work-order

# S1 work-order — the ladder generalisation (architect: Fable, read-only)

**Base facts (verified 2026-09-08).** HEAD is `e801f07` (plan + workflow committed on top of `1d988f5`) — I48(a)'s `<base>` is whatever S0 recorded, not `1d988f5`. Tree clean; no `v4-*` branch; three unrelated `.claude/worktrees/*` exist — leave them alone. `build --check`: lite total 583.9K, modelCode 52.1K/54K, appCore 359.3K/360K (682 B headroom); full appCore 359.4K. `node scripts/verify.mjs data/model.json --no-write` = 62/62 in 30 s (the flag must FOLLOW the path — bare `--no-write` is taken as the model path and crashes). Every line number the brief cites is still correct at HEAD.

**Order of operations.**
1. Record before-state: `sha256 data/model.json` and the three fixtures; `git status --porcelain` empty.
2. `policy.mjs` interface (all exported, all default `seats = 6`): `LADDER9`, `SEAT_DISPLAY` ({UTG1:'UTG+1', UTG2:'UTG+2'}), `seatsFor(seats)`, `POSITIONS = seatsFor(6)` (stays an exported plain array — jsmin's constants-parity test deep-compares it), `behindNonBlind(pos, seats)` (extend :764, keep the name; = non-blind seats after `pos`, 0 for blinds), `blindBehind(pos, seats)` (= blind seats after `pos`: 2/1/0), `isBlind(pos, seats)` (index ≥ seats−2), `nestChain(node, seats)` (rfi = non-blind seats; limps/raise = same minus first; 3bet = []), `positionDisabled(pos, node, seats)` (last seat closes rfi; first seat has nobody in front at limps/raise — build the two reason strings from the ladder, e.g. `` `no one acts before ${first}` ``, so they are byte-identical at 6 without a seat literal), `nMax(seats)` (6→7 frozen, 9→9), `ladderConstants(seats)` (6 → `{baseRaise: CONSTANTS.baseRaise, baseR: CONSTANTS.baseR}` BY REFERENCE so the 6 path reads the very property it reads today; 9 → built lazily from `ladder.earlyStep`/`baseRRule`, memoised on a key of its inputs so a gate that perturbs a legacy value cannot read a stale object), and the ring accessors of step 4. Delete `N_NB`/`N_BL`/`NEST_CHAIN`; the literals move verbatim into the witness test.
3. Threading `seats`: it is an ENV axis (§2.6 "seats joins env"). `envOf` reads `s.seats` (9 or 6, nothing else), `sealEnv` carries it, the `DEFAULT_ENV` fast-path condition gains `seats === 6`, `envKey` appends `|${e.seats}` unconditionally (no test or gate pins an envKey string — grepped; the solve/aggr/evCut memos all go through `envKey`, so nothing else needs a key edit). Readers: `nEff` (via `envOf(state).seats` for the tables and the clamp `Math.min(nMax(seats), …)`, `extrapolated: raw > nMax(seats) + 0.0001` — assert in the test that `7 + 0.0001 === 7.0001`, else keep a per-size threshold), `widthFor`/`width3For`/`depthWidthFactor` (env already in hand), `baseRealization(pos, d, seats)` and `realization(pos, N, nu, d, seats)` gain a TRAILING optional param (jsmin.test calls them with the old arity), `scoreCell` passes `e.seats`, `solve` uses `nestChain(node, e.seats)` for BOTH the active and the refSet cascades, `solve3bet` reads seats for heroIP. Nothing in `whyLines`, `derived`, `postPasses`, `tightenFor` is seat-count dependent (read them anyway and say so in the memo).
4. Ring accessors — S1 defines the payload SHAPE lane R must produce; policy never loads it: `ring = { meta: { nMax: 9, … }, cells: { [key]: { eq: [N8, N9], vDelta: { [v]: [N8, N9] } } } }`. `eqAtSeats(cell, N, seats, ring)`, `rhoAtSeats(cell, N, seats, ring)`, `vDeltaAtSeats(pts, vDelta, v, N, seats, ring)`: N ≤ 7 → delegate to today's `eqAt`/`rhoAt`/`interpolateDelta` (same code path, `===` identical at 6); N > 7 → require `seats === 9` AND a payload, interpolate between column 7 and the ring's 8/9, and THROW a TypeError naming `data/ring.json` when the payload is absent (fail closed — the I52 idiom; never fall back to `cells`). The trap to close: `rhoAt`/`interpAt` clamp silently at `rho.length = 7`, which is exactly the forbidden `cells` read at N > 7 with no error to surface it, and it lives in `scoreCell` (rho) AND in `villainEq`/`profiledModel` (the shadow rho/vDelta arrays are length 7). Thread `ring` as a trailing optional param from `rankTable`/`villainEq`/`profiledModel` (which hold `model`) down to `scoreCell`; the page will attach it as `model.ring` (lane U/R). At seats = 6 the param is undefined and no branch is entered.
5. Constants. `CONSTANTS.ladder = { seats: [6, 9], ladder9, display, earlyStep: 0.77, baseRaiseRule: 'geometric', baseRRule: 'flat'|'step', derived: { baseRaise: {UTG, UTG1, UTG2}, baseR: {…}, kind: 'estimate' }, anchor: '…', flag: '…≥60 chars naming I51…' }`. `earlyStep` is the LITERAL `0.77` — `1/Math.sqrt(1.25*1.35)` is 0.76980 and would break the `===` pin. `straddle` gains a sibling string `seatDerivedFrom: 'ladder.earlyStep'` (`seat` stays a number; I26 and `seatWidthFactor` read it). Pin `CONSTANTS.straddle.seat === CONSTANTS.ladder.earlyStep` in the test.
6. R1: run I51(a)'s clauses in a scratch harness under both rules (a: baseRaise strictly increasing at 9 — rule-independent; b: baseR non-increasing toward the front on the score surface and on `depthWidthFactor` at d=40/250; c: nesting at rfi over 66 VPIP × 12 lanes — note in the memo that the post-pass union makes nesting hold BY CONSTRUCTION, so (c) cannot discriminate). Expect both to pass → FLAT ships; record step's readings.
7. Offenders: :1406 → `L >= 2 && behindNonBlind(pos, seats) === 0`; :1914 → `behindNonBlind(pos, seats) <= 1 && !isBlind(pos, seats)`. Test the seat sets at 6 ({BTN,SB,BB}/{CO,BTN}) and 9. Both are expected to rewrite (offendersRewritten = 2).
8. `skill.mjs` keying, contention-safe: leave the two frozen arrays byte-for-byte and their names exported (`gates/skill.mjs` and `test/skill.test.mjs` read `.length` and the list — you do not own them); add `WIDTH_EXCEPTIONS = Object.freeze({ 6: { endpoint: WIDTH_ENDPOINT_EXCEPTIONS, interior: WIDTH_INTERIOR_EXCEPTIONS }, 9: null })` + `widthExceptionsFor(seats)`; give `legalPairs`, `widthTable`, `widthProblems` a trailing `seats = 6`. 9 stays `null` = "unmeasured, lane K" — never an empty array.
9. Six literal sites: `couplings.mjs:634`, `env.mjs:195`, `policy-sweep.mjs:37` → `P.nestChain(node, 6)`; `equilibrium.mjs:710` → `nestChain('rfi', 6)` (import it; I36's armed clause keeps its six-seat meaning); `baseline.mjs:247/:353` → the imported `POSITIONS`. Shell :3202 → `window.POLICY.nestChain(node, 6)` with the literal-free structural fallback `POSITIONS.slice(0, POSITIONS.length - 2)`; the fallback `evaluate` is dead once `POL` exists (reassigned at :3498), so the 3bet fallback delta is harmless — record it.
10. Shell fallback block :2182–2199: `POSITIONS` ← `window.POLICY.seatsFor(6)` when present (the literal stays as the marked legacy fallback — §10 Q3), `NNB`/`NBL` built over `POSITIONS` from `behindNonBlind/blindBehind(p, 6)`, `legalPos(node)` built from `positionDisabled(p, node, 6)`; keep `seatsBefore`. `N_EFF_MAX` → `function nEffMax() { return window.POLICY && window.POLICY.nMax ? window.POLICY.nMax(S.seats || 6) : 7; }` (`S` is declared at :2902, before :3156; `S.seats` is lane U's hook) and `nEffMax()` at the six use sites. MEASURE appCore after each shell edit — 682 B is the whole budget and S1 may not touch `variant.mjs`; aim for net ≤ 0 B (the three legalPos literal objects are the shrink).
11. Tests: NEW file `test/ladder.test.mjs` only (do not edit `policy.test.mjs`/`jsmin.test.mjs` — shared with lanes): every function vs the deleted literal, 21/33 legal pairs, `nMax(6) === 7`, the equality pin, `ladderConstants(6)` reference identity, nine-seat monotonicity, accessor delegation + fail-closed throw.
12. Measurements to the memo (scratch scripts in the scratchpad): R3 census on the re-derived domain — V3-BRIEF's 3,960 = (rfi 5 + limps 5×4 limpers + raise 5, 3bet EXCLUDED) × straddle 2 × 66 VPIP, 47 clamped = 1.19 % — recount it, then the nine-seat analogue (8 + 7×4 + 7 = 43 rows × 2 × 66 = 5,676) with per-(pos,node) shares; R4's enumeration (see risk 4 — the ONLY seats-in-front term is the nesting union; pre-nesting width is EXACTLY equal for shared seats, stronger than I50's monotone clause); step 5b on the v3-default surface (reuse `tier-fixture-v3`'s `sweepTiers`/shadow machinery with `seats: 9` in the state) — rfi is fully measurable (max raw 6.4 at UTG@9 straddled); at limps/raise the front seats UTG1/UTG2 read N > 7 even at 2 limpers (UTG1|limps ≈ 7.34, 8.2 straddled) and the accessor throws without a ring — count those settings as "ring-needed, S3", never proxy them by clamping.
13. Identity proof, in this order: `node scripts/verify.mjs` (WRITES model.json — see risk 2) → `node scripts/build.mjs` → `--check` both → `node --test test/*.test.mjs` → `node scripts/freeze-tiers.mjs --check`, `--v2 --check`, `--v3 --check` → `git status --porcelain` shows no `data/tiers-*` and no data file but `model.json` → memo ≤ 80 lines → snapshot ceremony exactly as briefed, then `git diff v4-s1-base` empty.

**What is a genuine blocker.** A fixture `--check` with a non-empty diff after a correct rewrite; `git stash create` printing nothing; appCore over 360 K that cannot be shrunk inside S1's three shell ranges. NOT blockers (summary only): model.json moving by `constants.ladder` + the straddle provenance string + `meta.hash`; a predicate that cannot reproduce at 6 (keep the literal, record the offender); R4/5b falsifying the plan's "other nodes exact" prediction.
RISKS:
- HEAD is e801f07, not the 1d988f5 the HOUSE text names; the I48(a) base is S0's recorded sha, and the three unrelated .claude/worktrees must not be touched.
- Brief leg (iii) 'model.json sha256 byte-identical' is unsatisfiable by design: verify.mjs's stampConstants republishes live P.CONSTANTS into model.json, so constants.ladder (+ meta.hash) lands on the first green verify; plan §0.4/§2.8 says that is the one permitted change — prove it by a key-by-key JSON diff and report it in summary, never as a blocker; the fixtures tolerate a hash move (P4/P5 precedent: model-hash is provenance).
- Plan §4's `straddle.seat gains derivedFrom` contradicts §0.4's 'outside constants.ladder identical'; follow §4 with a sibling string (seat stays a number for I26/seatWidthFactor) and record the wording exception.
- R4/I50 prediction falsified by code reading: nestChain(limps|raise, 9) = [UTG1,UTG2,LJ,HJ,CO,BTN], so HJ/CO/BTN gain three unions at limps and raise too, not only at rfi — I50 must be containment at three nodes; conversely no seats-in-front term enters widthFor at all, so pre-nesting width for shared seats is exactly equal.
- Silent cells read at N > 7: rhoAt/interpAt clamp at rho.length = 7 with no error, in scoreCell AND in villainEq/profiledModel's length-7 shadows — the accessor family (eq, rho, vDelta) must be threaded through all three and throw fail-closed without a ring payload; S1 fixes the payload shape lane R generates.
- 5b at limps/raise cannot be fully measured at S1: front seats UTG1/UTG2 read N > 7 at the default 2 limpers and there is no ring yet; measure rfi fully, count the rest as ring-needed for S3's sub-ladder diff, never clamp as a proxy.
- earlyStep must be the literal 0.77 (1/sqrt(1.6875) = 0.76980 breaks the === pin); and `nMax(6) + 0.0001` vs the literal 7.0001 must be asserted equal or the extrapolated flag could flip at an ulp.
- envKey gains a seats field unconditionally — no pinned key strings exist, but any memo (solve, aggressiveSet, evCut) missing it hands back a six-seat answer for LJ..BB at nine, silently.
- appCore has 682 B of headroom and S1 may not touch variant.mjs: the fallback-block rewrite and nEffMax() must be near byte-neutral, measured after every shell edit.
- I51(c) as literally worded ('nine keys only inside LADDER9/seatsFor/fixtures/display map') is already violated by ~30 pre-existing literals in gates and tests (couplings.mjs:408, env.mjs:523/615, policy-sweep.mjs:279, policy.test.mjs etc.); S1's memo must hand lane F an allowlist baseline so the gate asserts 'no NEW literal'.
- ladderConstants(9) memoised naively goes stale when a gate perturbs a legacy value (I26 mutates straddle.seat; depth/couplings gates read baseR live) — key the memo on its inputs, and read earlyStep from ladder, never from straddle.seat.
- constants.ladder.flag will name I51 before I51 exists (harmless, couplings' flagProblems is called per named block only) and the Method view's UNANCHORED badge entry for ladder.derived is lane U's — say both in the memo so nobody infers S1 forgot them.
- tightenFor's default raiser 'CO' at nine seats (HJ|raise with the raiser behind) is a pre-existing six-seat quirk, not a new exclusion — record, do not fix, and it keeps the legal-pair count at 33.
- Test-file contention: only a new test/ladder.test.mjs is safe; policy.test.mjs and jsmin.test.mjs are shared with the S2 lanes and jsmin parity for the new exports is left as an S3 note.
- verify.mjs's --no-write must follow the model path (`node scripts/verify.mjs data/model.json --no-write`); the bare form crashes and the write form is what the identity proof needs anyway.

## S3-integration work-order

# S3 work-order — integration + the fixture freeze (architect: Fable, read-only; verified 2026-09-08)

**Base facts.** main HEAD e801f07, index fully staged with S1's work; `git diff v4-s1-base` (tracked) is EMPTY; one untracked stray `docs/spikes/V4-S6-verification.md` (a run-1 S6 report that says RED — not yours, do not `git add -A`, note it in the memo for S5/S6 to remove). `v4-s1-base` = c76720d (parent 33a228f, a stash-shaped WIP commit; e801f07 is an ancestor, so I48(a)'s fallback base resolves). All four lane tips have merge-base c76720d exactly and none touches policy.mjs, V4-PLAN.md, v4.js, model.json or a fixture (`git diff --stat v4-s1-base <lane> -- <those>` empty for all four). `git merge-tree` predicts: R∪U conflicts ONLY in index.html and index-full.html (both generated — take either side, they are rebuilt in step 4); src/shell.html is "changed in both" and merges clean; every other lane pair is conflict-free. Ring artifact measured: 18,607 B on disk, 18,620 B as `const RING = …;` (note 505 · meta 1,469 · cells 15,362 · agree 1,235); `ring.meta.v` carries the lattice points.

**Step 1 — normalise.** `git reset --soft v4-s1-base` (index already equals the snapshot). The amend rewrites c76720d to a new sha while the lanes still descend from the OLD one, so the merge base becomes 33a228f; both sides carry the identical R2 hunk, so it still merges clean — preserve the original body (`-m "<subject>" -m "$(git log -1 --format=%B v4-s1-base)"`) so the owner's amendment provenance survives, record both shas, re-run `git merge-tree` against the amended HEAD before merging, and confirm `git status --porcelain` shows only the `??` stray.

**Step 2 — merge R, U, F, K (`--no-ff`).** index*.html: take a side, never hand-merge. After U: re-verify the six owned shell regions byte-for-byte (S1 :2182–2199/:3155/:3202, R :1181/:1318/:1407 = `SIM_NMAX`/`setSimSeats`/`validEqArray(e,n)`/`p.nMax !== SIM_NMAX`). After F: `EXPECTED_IDS.length === 69`, the 62 a strict prefix, importing `scripts/gates/index.mjs` does not throw; a conflict in index.mjs/reserved.mjs is a report, not a merge. After K: `test/equilibrium.test.mjs` red is expected until `node scripts/generate-equilibrium.mjs` (+22 B, baselineTiers unchanged) — run it once in step 6's sequence, then `--check`. Every verify run stamps data/model.json: use `node scripts/verify.mjs data/model.json --no-write` (flag AFTER the path) while any red is open, or `git checkout -- data/model.json` after.

**Step 3 — policy deltas, serially, order F1 → U2 → F3 → F2 → F4 (report policyDeltasApplied = 5).** After each: I48 + the three legacy `--check`s + modelCode bytes (cap 57,344 B, S1 left 1,074 B of headroom — measure, and a raise if forced is ≤ ceil(×1.08) with its shrink sentence).
- F1/U1: `ring: state.ring` in `solveUncached`'s `opts` — ONE object feeds both the active and the refSet cascade, confirm by reading :2300–2306. The `solve3bet` half of F1 is REFUSED with reason: vs-3bet's N_eff is the constant 2 and `vs3betCuts` never reads N, so the ring is unreachable there (a delta only a lane's tidiness wanted).
- U2: the same one line in `evCutUncached`'s `aggressiveSet` call (else EV mode goes dark at nine seats by name).
- F3 (`vDeltaAtSeats(pts, vDelta, v, seats, ring, key)` + ring through `villainEq`/`profiledModel`): my call is the ACCESSOR-SIDE design — `profiledModelUncached` stamps `nc.vp = p.v` on SHADOW cells only (never on `model.cells`, never serialised), and `ringCols` for a `vpSource` cell returns `r.eq[i] + interpolateDelta(ring.meta.v, pts.map(p => r.vDelta[p]), cell.vp)[i]`, exported as the named helper. `villainEq` keeps returning 7-long arrays, the model path and `tier-fixture-9max.mjs`'s `profiledModel(model, profileFor(...))` call need no change, and a 9-long `eq` never reaches `NMAX`'s invariant. Keep the throw for `vpSource === 'measured'` (a 9-wide Simulate result is dropped by `villainEq`'s `m.length === cell.eq.length` today — record that as a limitation for S5, do not widen the delta). Re-point `test/ladder.test.mjs:220` ('refusing to mix') to the measured case.
- F2: SOLVE_MEMO/AGGR_MEMO (and EV_MEMO) keys gain `|${ring && ring.meta ? ring.meta.contentHash.slice(0,8) : ''}` — inert at six, closes the I52 tripwire's stale-memo trap.
- F4: `CONSTANTS.ladder.census = { domain: 6336, clamp: 9, clamped: 19, byPair: { 'UTG1|limps': 14, 'UTG2|limps': 5 } }` as a typed MEASURED RECORD (the WIDTH_EXCEPTIONS idiom; the keys match gates/ring.mjs `censusOf`'s `pos|node`; policy.mjs is on ALLOW), comment naming I52(c) as the recount. Check model.json's `ladder` block stays ≤ 1,024 B (515 B + ~80). Stamping is `verify.mjs`'s `stampConstants` — never generate-data.mjs.

**Step 4 — register the ring, pay the budgets (numbers to MEASURE, not copy).** Expected readings from the memos: appCore lite 368,471 + R's 373 ≈ 368,844 B vs 368,640 (360K) → OVER by ~200 B, full ≈ 368,996 → over by ~356 B, before S3's own bridge line; blocks.ring 10,846 B; total lite ≈ 613 K + 18.6 K payload ≈ 617 K vs 600 K; total full ≈ 668.3 K + 18.6 K + 22 B ≈ 687 K vs 660 K.
- `scripts/lib/variant.mjs`: `regions` += 'ring' in BOTH; `ring: 20 * 1024` in BOTH `budgets` (D12(d) prints exactly this); `blocks.ring: 11 * 1024` (measured 10,846; bound 12K; 11K is the tighter reading, re-measure after the merge); appCore shrink-first (lane U already recovered 308 B, S1 −209 net, R 241 — quote them), then a paid raise to 361K if still over; `app = appCore + 49K` in BOTH (equality pin at test/variant.test.mjs:280/:285); total raised in BOTH at cap ≤ ceil(measured×1.05), chosen tight (+~0.5%, the P5 precedent). Every raise's budgetSource sentence carries the shrink in BYTES: lane R's measured 3,075 B (vDelta as arrays — my call: DECLINED, it changes the artifact shape S1's accessor contract fixes and costs a 1,255 s regeneration) and the 1,740 B projection (`note` 505 + `agree` 1,235 not needed on the page — my call: DECLINE too and inject the file verbatim on the eq precedent; if you take it, `injectedForm` must be ONE export in ring.mjs used by build.mjs and D12(d)). Append the new prose INTO existing budgetSource lines (S1's trick) so D6's cites at variant.mjs:104/119/136/140/159/224 still resolve; if a line must be added, re-pin those cites in `gates/data.mjs` CEILING_MARGINS and say so.
- `scripts/build.mjs`: read data/ring.json for BOTH variants (die if absent, the eq idiom), `blocks.ring`, banner line AFTER the shell-hash line (D11 takes the first `sha256`), report `+ ring x.x`, `sizeProblems` on `BUDGETS.ring`, and `app = total − data − modelCode − eq − ring`. `scripts/lib/block-census.mjs`: `BLOCKS` += 'ring', `APP_EXCLUDED_REGIONS` += 'ring', pageCensus returns `ring` and subtracts it — build and D6 must read the same arithmetic.
- `src/shell.html` (your minimal, recorded touch): a `<script>/* @inject:ring */ /* @end:ring */</script>` beside `@inject:eq` but OUTSIDE any `@only`, plus a bridge line `window.MODEL.ring = RING_PAYLOAD`. THE IDENTIFIER MUST NOT BE `RING`: lane U declares `var RING = null` at :2976 inside the main script and reads `window.RING` at :7078 later in the SAME script — a global `const RING` is a SyntaxError against that `var`, and `window.RING = …` is nulled by the `var` before the read. Lane U's second branch, `MODEL.ring`, is the seam that works and is S1's stated contract. Make D12(d)'s `injectedForm` use the same identifier. Also the 327→328 comment fix at :7059.
- Tests: `test/variant.test.mjs` budgets deep-equal + `MARKED.ring` (9 markers in lane U's shell) + regions; `test/block-census.test.mjs` `TODAY` re-pinned ONCE after the FINAL build; `test/gates-reserved.test.mjs` `PENDING_V4` deleted after the first clean 69-stamp verify (its own comment says so). `gates/data.mjs` BUD: no edit — S1 already paid meta 20K/total 146K/ladder 1K (plan §2.7's "no edit" prediction was falsified at S1; keep it recorded), `core` 120K untouched.

**Step 5 — I50 clauses (before the freeze).** Lane F's skeleton already scores (i) tiers off the two frozen files, (ii) containment with excess attribution, (iii) pre-nesting monotone. Finalise the per-node list from S1 §5–6 and F §2.4: containment at rfi AND limps AND raise (`nestChain(limps|raise, 9)` = UTG1..BTN, so HJ/CO/BTN gain unions there too — the plan's rfi-only prediction is refined, not patched); SB exact at rfi, SB/BB exact at limps/raise, all six exact at 3bet; pre-nesting measured EQUAL (0 of 16,272) — assert monotone as written, print equality. Expect i50RfiExact = false (42 strict supersets at rfi, 1,210 at raise, 0 subset violations, 0 unexplained). Annotate §5.2 I50 and §8 risk 1 with `> **Measured (stage S3).**` blocks (raise bites hardest; nut-gate bypass real, 0 of 1,046 load-bearing). Report i50Clauses as the number you actually assert.

**Step 6 — the ceremony, in this exact sequence.** `git checkout -- data/model.json` → `node scripts/generate-equilibrium.mjs` → `node scripts/verify.mjs` (stamps `constants.ladder.census`; I49/I50(i) red and stamped FAIL — expected) → `test ! -f data/tiers-9max.fixture.txt` → `node scripts/freeze-tiers.mjs --seats9 2>&1 | tee <scratchpad>/freeze.log` EXACTLY ONCE (expect 26,136 settings, 809–845 KB, ~6 s) → `--seats9 --check` REPRODUCES → `node scripts/verify.mjs` again (all 69 pass, stamps) → `node scripts/build.mjs` both → `--check` 2/2 → `node --test` → three legacy `--check`s → smoke (three morph rows 8/16/4 unmoved + the toggle row) → browsers → the three generator `--check`s byte-identical. A verify with a red stamped, followed by build, reads as a byte-ceiling failure (build.mjs:185) — restore model.json, do not touch budgets.

**Step 7 — the sub-ladder diff.** Full freeze output + per-seat union counts → `docs/spikes/V4-integration.md`; the summary → METHODOLOGY APPENDED AT THE END (after :3670) under "S3 handoff — the sub-ladder diff (S5 folds into §3.6)" so no cited line moves (D6 cites up to :2636, I36 :3405, couplings' limitation slices); re-run verify after.

**Step 8 — domain checks.** I36(b) stays `nestChain('rfi', 6)`, (d) reads 3 of 36; I38(d)/(e) in `gates/skill.mjs` (nobody's file — yours now): add `widthProblems(model, SKILL_GRID, 9)`, `legalPairs(9)`, and thread `seats` into BOTH the (e) probe's `P.solve` and `P.realization` (expect 14,760 readings, 0 mismatches; 5,535 false mismatches if realization stays seats-blind); I15 via I51, I26 unchanged. I51(c): add `scripts/lib/skill.mjs` and `test/skill-9max.test.mjs` to ALLOW with the reason (frozen measurement records + their pins), never move LEGACY_PIN 655 — lane K's 13 lines are that case and the gate firing is the decision being taken in the open.

**Step 9.** No commit beyond step 1's bookkeeping amend; nothing pushed.

**Genuine blockers:** `git diff v4-s1-base` non-empty; the fixture already existing; a conflict in index.mjs/reserved.mjs; a delta that moves a legacy fixture `--check`; the freeze REFUSING (settings printed as refused) after the deltas; an appCore/total that cannot be capped ≤ ceil(measured×1.05); a `--seats9 --check` that does not reproduce. NOT blockers: i50RfiExact false; a settings count ≠ 26,136 without refusals (annotate); the S1-era falsifications; the 9-wide measured-path limitation; WALL_MEASURED_MS near its soft ceiling (S5 re-measures).
RISKS:
- The `const RING = …` injected form lane R measured collides with lane U's `var RING = null` (shell :2976, same main script as the :7078 read): a global const is a SyntaxError against that var and a window.RING is nulled before it is read — inject under another identifier and bridge to MODEL.ring, or the 9-max control is dead on a page that carries the ring.
- No lane wrote the `@inject:ring` markers or the bridge line — regionManifest throws for both variants until S3 adds them to src/shell.html, and that shell edit costs appCore bytes on a row already ~200-350 B over 360K after R+U.
- appCore is expected OVER 360K after the merge (U measured 19 B of full headroom before R's +375 B): the raise to 361K must quote U's 308 B / R's 241 B / S1's 209 B shrink record in bytes or D13 fails on the sentence, not the number.
- pageCensus and build.mjs each compute `app`; if only one subtracts the new ring region, D6 and the build disagree by 18.6 KB and appCore reads over budget in one of them.
- The step-1 amend rewrites c76720d while the lanes still descend from the old sha, moving the merge base to the stash commit 33a228f; the identical R2 hunk on both sides keeps it clean, but re-run merge-tree after the amend rather than trusting the pre-amend prediction.
- Step 4b of the brief says gates/data.mjs BUD needs no edit, but S1 already raised meta 19->20K, total 145->146K and added ladder 1K (§2.7's prediction falsified at S1) — do not revert it, and do not raise `core` 120K.
- D6 cites variant.mjs by literal line (:104/:119/:136/:140/:159/:224) and METHODOLOGY up to :2636: adding a line to budgetSource or inserting the sub-ladder subsection mid-document reddens D6 at S3 unless the cites are re-pinned or the text is appended in place/at the end.
- verify.mjs stamps every verdict into data/model.json and build.mjs refuses a stamped non-pass: verify-then-build with I49/I50 open looks like a byte-ceiling failure; the only correct sequence is equilibrium regen -> verify -> freeze -> --check -> verify -> build.
- F3's design is the run's largest open decision: routing profiled ring columns through the accessor (shadow-cell `vp` + interpolation over ring.meta.v) keeps villainEq 7-long and lane F's fixture library untouched, whereas a 9-long villainEq eq ripples into NMAX, hydrate, tier-fixture-9max and the shell — pick the accessor route and record it.
- A 9-wide Simulate measurement at nine seats is silently dropped by villainEq's `m.length === cell.eq.length` and never reaches the tiers: not a crash, but a limitation S5 must write down rather than S3 widening F3 to fix.
- I51(c) WILL fire on lane K's 13 measured-record literals in skill.mjs and its tests; resolve by adding those files to ALLOW with a reason, never by moving LEGACY_PIN 655 — and K's own count is per-line while F's scanner is per-occurrence, so re-read the gate's number, not the memo's.
- test/gates-reserved.test.mjs pins the unstamped set to exactly the seven pending ids; the first clean 69-stamp verify flips it red until PENDING_V4 is deleted — expected, and the test's own comment says so.
- modelCode has ~1,074 B of headroom under 56K before five policy deltas land; measure after each, and a forced raise is ≤ ceil(measured×1.08) with its sentence, re-pinning block-census.test's `model code 55.0K/56K` regex.
- gates/skill.mjs I38(e) extended to nine seats without threading `seats` into P.realization reports 5,535 false mismatches — a gate written to fail on correct work if half-extended.
- Lane K's shipped `see SIXMAX` CAPS.omitted string is table-size prose in four artifacts, but renaming it moves model.json's baselineTiers.caps outside constants.ladder (§0.4): leave it this run and file it, do not fix it at S3.
- The ring's D12(e) margin is 24.6 s (1.9%) against 3.4% run-to-run variance: never regenerate data/ring.json at S3 — any kernel/generator edit reddens D12(a) by hash and forces a 21-minute rerun that may not fit.
- The stray untracked docs/spikes/V4-S6-verification.md (run-1, says RED) will ride into S6's commit if anyone `git add -A`s; flag it in the memo rather than silently deleting or committing it.
- I49 adds ~5.7 s once the fixture exists, putting the suite near the 41,920 ms soft wall ceiling — informational (never changes the exit code), S5 re-measures WALL_MEASURED_MS.
- test/block-census.test.mjs TODAY is a four-lane race: re-pin it exactly once against the FINAL build, after every budget and shell edit is done, or it is re-pinned twice.
